# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Is

**Rynek Proroctw G6** — a company prediction market web app with virtual coins, a shared Texas Hold'em table, and small office games. Polish UI. Single static file (`index.html`) backed by a hosted Supabase project and Supabase Edge Functions for server-owned game actions.

Live URL: `https://inlineskater.github.io/rynek-proroctw-g6/`

## Deployment

There is no build step. Push to `main` → GitHub Actions copies `index.html`, `login_hero.jpg`, `games/` and `tabs/` to GitHub Pages, stamps the commit SHA into `BUILD_ID`, and writes `version.json`. The one gate is the **payload budget check**, which fails the deploy if `index.html` is over 1600 KB (see Architecture). That's the entire pipeline.

To apply database changes: paste the relevant SQL into the Supabase SQL Editor (Dashboard → SQL Editor → Run). There is no migration runner.

Deep-dive design docs that don't need to load on every task live in `docs/` — read the relevant one before making non-trivial changes to that feature: `docs/db-migrations.md` (full per-file migration rationale for every `supabase/*.sql`), `docs/seasonal-games.md` (per-game tuning history), `docs/healer-dungeon.md` (full sim/balance design), `docs/filler.md` (1v1 flood-fill: schema, matchmaking races, anti-farming), `docs/all-games-picker.md` (arcade storefront preview engine), `docs/farma.md` (farm gameplay/economy — grid, lootboxes, NFTs, pricing, land tax, marketplace), `docs/farm-ui-conventions.md` (farm frontend/UX conventions), `docs/bank.md` (Bank G6 investment products — rate/price derivations and the caps that keep them safe), `docs/anti-inflation.md` (the 2026-08-28 economy audit: why the farm was an open loop, and the four controls that closed it), `docs/hilo.md` (Drabina Kariery: the martingale ladder and why the edge is taken once), `docs/tower.md` (Wieżowiec G6: the same martingale, with no ceiling clamp and stochastic payout rounding), `docs/coinpusher.md` (Automat Monet G6: real rigid-body physics with a closed-loop coin inventory instead of server-side outcomes), `docs/BACKUPS.md` (data backup setup).

- `supabase/schema.sql` — core prediction-market schema, run once on a fresh project
- `supabase/arcade.sql` — free-forever „Wszystkie Gry" archive scores (SELECT-only for clients); `pay_arcade_entry` just validates auth/game_type (no coin cost); `record_arcade_score` validates the score cap and rate-limits to 1 submission/5s per user+game as the anti-spam throttle. **Adding an arcade-only game means adding its `game_type` in TWO places here** — `pay_arcade_entry`'s whitelist and `record_arcade_score`'s cap `CASE` — then re-running the file (idempotent). Miss the first and the round never starts; miss the second and it plays fine but every score is rejected on submit. (Filler is the exception: its Edge Function writes `arcade_scores` directly as `service_role` and bypasses both RPCs.)
- `supabase/store.sql` — reward shop tables/RPCs used by the Sklep tab
- `supabase/bank.sql` — **„Bank G6"** investment products on their own nav tab (`#tab-bank`, UI in `tabs/bank.js`): **🏦 Lokata** (hard-term deposit, 7d/+5% · 14d/+12% · 30d/+30%, **dynamic** cap, breaking early returns principal and forfeits interest), **🐷 Skarbonka** (no term, 0,60%/day simple, **dynamic** cap, interest locked for 7 days), **📜 Obligacje** (weekly series of **dynamic** size; 1 000 face, 8/day coupon, 20 days, **tradeable**, allowance `ceil(edition/active players)` per player, lifted in the issue's last 2 days), **🎰 Udział w Kasynie** (30 shares × 4 000, each paying **3% of the trailing-7-day positive casino house net** — the one product that mints nothing, only redistributing coins already burned, and pays 0 in a week nobody gambles), and **💍 „Sygnet Bankiera"** (`banker_signet`, 15 000 🪙 in the Sklep — the legendary ring's deal exactly: **2%/day of the whole cash balance, uncapped**). **LIMITS ARE DYNAMIC**: `bank_ensure_limits()` recomputes every per-player cap once a Warsaw day and freezes it in `bank_limits` — budget = 0,30% of cash supply × a health multiplier `TARGET/(TARGET+inflation)`, split 55/10/35 across lokata/skarbonka/obligacje and divided by active players, then clamped. So the Bank throttles itself when the game is already minting fast (on 2026-08-23 the economy was net-minting **+9 685/day = +2,68%/day** from the farm and lottery, giving health 27% and a lokata cap of 4 500 instead of the 16 000 a healthy economy allows). Rates are NOT dynamic and every position stores its own terms, so a limit change is never retroactive. ⚠️ **The interest base for `daily_interest` items is cash PLUS open Bank deposit principal** — cash-only made the Bank a trap (locking the max lokata earned +630 while forgoing 2 700 of Sygnet interest, a net −2 070, so the rational play was to use nothing); counting deposits makes every product purely additive. Don't simplify it back to `p.coins`. ⚠️ Budget fixes `cap × rate`, not either alone, so doubling rates and halving caps costs the economy the same and reads far better — that is why 30-day lokata shows +30%. Adds `bank_deposits`/`bank_bond_series`/`bank_holdings`/`bank_dividends`/`bank_limits` (SELECT-only for clients) and `bank_state`/`bank_open_deposit`/`bank_close_deposit`/`bank_buy_bond`/`bank_buy_share`/`bank_list_holding`/`bank_unlist_holding`/`bank_buy_holding`. **Settlement is LAZY-ON-READ** (`bank_settle_due()` at the top of `bank_state`, crash/wheel pattern) with an hourly cron at **minute 35** as a backstop — idempotent and date-driven with a 14-day lookback, and bond redemption pays any coupon day the accrual missed, so a cron outage delays payouts instead of losing them. Also **rewrites `award_daily_interest()`**: it pays any def with `effect_type='daily_interest'` at that def's own `effect_value`, honours `hero_item_defs.interest_cap` (NULL = uncapped, which both interest items are today), and pays **only the best single item per user** so the ring and the Sygnet cannot stack to 4%/day. ⚠️ **The Sygnet is ~85% of the whole feature's coin creation** (~7 215/day if all 11 players hold one, compounding, vs ~1 250/day from all four products combined) — it is the only unbounded term, and `interest_cap` on that one row is the knob, not the other products' limits. ⚠️ Every `profiles` update inside `bank_settle_due()` must aggregate per user first — `UPDATE profiles p SET coins = p.coins + x.amount FROM cte x WHERE p.id = x.user_id` updates a row **once** when the CTE holds several rows for it, silently dropping the rest (it lost 24 of 48 coupon coins in this file's first smoke test while the dividend table still recorded all 48 as paid). ⚠️ The primary share sale needs `pg_advisory_xact_lock`, not a row lock on the buyer's own profile, or the 30-share float becomes 31; and the `redeemed_at IS NULL`/`closed_at IS NULL` guard must sit on the UPDATE, not only in the CTE that selected the rows, or a concurrent settle pays twice. Run order: `bank.sql`, **then** re-run `leaderboard-net-worth-items.sql` + `economy-stats.sql` + `coin-inflow-stats.sql` (all three now call `bank_user_assets()`/`bank_total_assets()` and will fail without it). Full rate derivation, the measured prod baseline, the inflation table and the tuning knobs are in docs/bank.md — read it before changing any rate, cap, or price.
- `supabase/anti-inflation.sql` — **run LAST**, after every farm file and after `bank.sql`; idempotent. The 2026-08-28 economy audit's four fixes. **⚠️ Re-run it after re-running `farm-collector-perks.sql` (owns `sell_crop_to_npc`), `farm-price-history.sql` (owns `roll_farm_prices`/`snapshot_farm_prices`) or `bank.sql` (owns `bank_net_mint_per_day` + the Sygnet seed row)** — each of those reverts part of this file. **The farm was the one OPEN LOOP in the game**: `roll_farm_prices()` never read `total_sold` (price was pure `random()`, re-rolled twice daily, and the roll reset `cur_price` *up*), the per-sale dip fully recovered in 8h20m (`LEAST(1, hours*0.12)`), and yield is `base_yield × (1 + (L−1)×0.5)` — linear and unbounded in level with grow time floored at 24h. Measured: the farm minted **54,722 🪙/day against ~14,300/day of recurring sinks**, i.e. it reprinted the whole money supply every 7.4 days; box/level-up spending looked like the balance but was a build-out phase that had already collapsed 800k→134k per week. Four changes: **(1)** `bank_net_mint_per_day()` → **trimmed mean** of daily buckets (one 779,493 lottery row owned the Bank's health metric for 25 days: 10,837 → 2,793, health 0.26 → 0.59, bond edition 4 → 11, and it defuses the 2026-09-03 cliff); **(2)** `interest_cap` = 20,000 on both `daily_interest` items — the Sygnet was the only unbounded *compounding* term (2%/day of the whole balance), now 400/day flat; **(3)** the weekly contract premium is capped at `fair_cap_user_units × farm_seasonal_premium_cap_x()` per player — `bonus_per_unit` is derived for a **level-1** farmer and was paid on unlimited units, so whales collected ~25× the design target while everyone under the cap was untouched; a new `farm_seasonal_event_sales.premium_qty` keeps the community bar and rank race counting **full** output, so a capped seller loses only the top-up, never the sale; **(4)** the stalk market gets a demand side — `budget = max(floor/player, 0.9 × measured burn)`, `demand = clamp(1/(1+max(0,pressure−1)), 0.20, 1.00)` applied to `anchor_price`, `cur_price` and a new `farm_market.floor_price`, with a ±0.15/roll **glide** and a per-crop skew. Above pressure 1, crop revenue is asymptotically **constant in quantity**, so card level stops driving coin creation — nobody's cards are nerfed. **The budget tracks BURN, not supply, on purpose**: supply-linked would be positive feedback, burn-linked means the office's spending funds its own income, and the per-player floor sits below the unconditional land-tax+casino burn so it can't spiral. Adds `farm_market_pressure` (public SELECT) + `farm_market_state()` for the 📈 Ceny banner. Every policy number is a one-line `IMMUTABLE` function (`farm_npc_burn_share`, `farm_seasonal_premium_cap_x`, `farm_npc_budget_floor_per_player`, `farm_crop_skew_exponent`, `farm_demand_bounds`). ⚠️ Client mirrors in `index.html`: `farmMarketFloor()`, `FARM_SEASONAL_PREMIUM_CAP_X`, `farmSeasonalPremiumLeft()`, and `floor_price` in all three `farm_market` SELECTs. Full audit, derivations and simulations in docs/anti-inflation.md — read it before touching any of those knobs.
- `supabase/office-goals.sql` — **„Wspólny Cel Biura"**: a communal coin BURN that unlocks something for everyone. Run after `farm.sql`, `garden-zen-split.sql` and `anti-inflation.sql`; idempotent. **⚠️ Re-run after re-running `garden-zen-split.sql` (owns `buy_farm_tile`) or `anti-inflation.sql` (owns `farm_burn_per_day`).** Built as a SERIES, not a one-off: `office_goals` holds many, one open at a time (a real partial unique index), each with a `reward_code`; adding the next goal is one INSERT. Adds `office_goal_contributions`, `office_goal_contribute()` (advisory-locked, clamps the last payment so nobody overpays a finished goal, coins burned not escrowed — there is no refund path and no "goal failed"), `office_goal_state()` and `office_goal_reward_active()`. The first goal, **`farm_row_5` at 150 000 🪙, unlocks 5 more farm plots** — the board is full (40 of 40 owned) and under the demand-throttled market more tiles no longer mean proportionally more minting, so selling land for burned coins is safe in a way it wasn't before. **`farm_extra_tiles_unlocked()` is the single source of truth for the row-0 boundary** — `buy_farm_tile`'s guard (`p_y = 0 AND p_x >= …`), `farm_normal_tile_capacity()` (and therefore the land-tax fair share, 4 → 5) and the frontend's `farmExtraTiles()` all derive from it. `office_goal_contribution` is in `farm_burn_per_day()`'s reason list on purpose, so **funding the goal raises everyone's crop prices** — that composition is the whole reason the NPC budget tracks burn instead of supply. No per-player cap: if one player wants to buy the office five plots that is a 150 000 burn and a fine outcome; the contributor board is what makes it worth their while. Frontend: `#og-goal` bar above the farm stage → `openOfficeGoalModal()`, realtime on `office_goals`, and the new plots render as a **9th column on the right** via `farmDisplayCoords()` (it interleaves one unlocked plot at the end of each display row; the mobile 7-col reflow falls back to appending).
- `supabase/hilo.sql` / `supabase/functions/hilo-action` — **„Drabina Kariery G6"** 🃏 (internal gameType `hilo`), a solo house game: a Hi-Lo card ladder. The server shows a card, you call WYŻEJ (higher **or the same**) or NIŻEJ, with the TRUE probability printed on the button; right multiplies the pot by the fair `1/p` and flips the next card, wrong ends it, cash out whenever. Ties win **both** calls, so `P(hi) + P(lo) = 14/13`. ⚠️ **The house edge is applied ONCE at cash-out, never per step** — each step multiplies by exactly `1/p`, so the ladder is a MARTINGALE: RTP is a flat 95% at every streak length and no strategy beats another. Applying `HOUSE_FACTOR` per step would compound to 0.54 over 12 steps and punish exactly the runs the game exists for. **No secrets table, deliberately**: unlike mines/crash there is no state the player bets against before acting — each card is drawn from crypto RNG at the moment of the call and is immediately public. The cap that matters is in COINS (`MAX_PAYOUT` 150 000), not multiplier, and `capMultiplier(bet)` is shown in the UI because a cap truncates the ladder's fair value and is the one thing that can make the published odds a lie. Adds `hilo_rounds` (own-row SELECT, one active per user via partial unique index) / `hilo_spins` (public — the live feed is most of why anyone plays twice) plus `hilo_week_streaks` / `hilo_all_time_streaks` / `hilo_recent`, ranked on **streak** not coins won. Gets the casino-luck amulet like the other house games. Run order: `hilo.sql`, **then** re-run `hazard-views.sql` + `coin-inflow-stats.sql` + `economy-stats.sql`, then `supabase functions deploy hilo-action`. Frontend is `tabs/hilo.js` (lazy module; `index.html` holds only the `#tab-hilo` shell, the `.hl-*` CSS and the `stopHiloTimer()` stub). Full design in docs/hilo.md.
- `supabase/tower.sql` / `supabase/functions/tower-action` — **„Wieżowiec G6"** 🏗️ (internal `tower`), a solo stacking house game: before each floor you pick a block — 🧱 Szeroki 90% ×1,11 · 🏢 Normalny 70% ×1,43 · 🗼 Wąski 45% ×2,22, TRUE odds on the button — the server rolls the landing, the canvas plays it out, and you sell whenever you have a floor. Built as option A (server luck + risk choice) on purpose: a real timing Stacker would let good reflexes beat 100% RTP, i.e. an open loop. Same martingale as Hi-Lo (fair `1/p` per floor, 5% taken ONCE at cash-out), plus two fixes Hi-Lo lacks: ⚠️ a block whose next multiplier would cross the coin ceiling is **not offered** rather than clamped (clamping quietly makes that floor unfair; when nothing fits the tower auto-cashes, `result: "capped"`), and ⚠️ the payout is **stochastically rounded** (`settlePayout`: floor + Bernoulli(frac)) because a plain `floor()` leaked up to 3.5 RTP points at a 10-coin stake — the displayed pot is the floor, so nobody is ever paid less than shown. `scripts/tower-rtp.mjs` verifies RTP EXACTLY (recursion, not Monte Carlo) for 7 strategies × 4 stakes × both house factors, extracting the pricing code from the Edge Function. The landing `offset` is rolled server-side too, so a reloaded tower and the feed show the same shape. Adds `tower_rounds` (own-row, one active per user via partial unique index) / `tower_spins` (public) + `tower_week_heights` / `tower_all_time_heights` / `tower_recent` (ranked on HEIGHT of cashed towers). Gets the casino-luck amulet (0.98). Run order: `tower.sql`, **then** re-run `hazard-views.sql` + `coin-inflow-stats.sql` + `economy-stats.sql` + `last-active.sql`, then `supabase functions deploy tower-action`. Frontend is `tabs/tower.js` (lazy module that also injects its own CSS; `index.html` holds only the `#tab-tower` shell and the `stopTowerTimer()` stub). Full design in docs/tower.md.
- `supabase/coinpusher.sql` / `supabase/functions/coinpusher-action` — **„Automat Monet G6"** 🪙 (internal `coinpusher`), a **physical 3D coin pusher**: every coin is a Rapier rigid body (`games/coinpusher-core.js`, shared by the browser and `scripts/coinpusher-sim.mjs`), a kinematic pusher block moves coins only by contact, two tiers (block top → bed), side gutters vs a front prize chute, rendered with three.js in `tabs/coinpusher.js`. **The server never decides what falls — it bounds the CLAIM:** each player owns one persistent machine whose every coin is a `coinpusher_coins` row with a server-issued id; `collect` pays an id once (status-guarded UPDATE) and only if it is in the caller's machine; gutter coins are house revenue, `RECYCLE` 40% of which funds that machine's `house_bank` (cap 3 000), and **everything that is not a stake (refills, 🟡 gold ×5, 💎 jackpot, 🌧️ rain) is paid for out of that bank at issue** — so lifetime payout ≤ stakes + one 140×1🪙 starter pile, and a client that claims every coin as a prize nets exactly the starter (measured on Postgres). ⚠️ **Never fund anything in it from outside the player's own machine** (a shared jackpot, a minted bonus): that loop IS the safety argument. ⚠️ **Stats are session rows booked at SETTLEMENT** (`bet += Σ funded` of resolved coins, `total_won += Σ prize`), never per drop — a per-drop row books a coin still in the machine as burned and inflates `farm_burn_per_day()`; consumers read `updated_at` as the row's time; it is deliberately **not** in `bank.sql`'s casino-share basis. Also: a per-tab **lease** on every write, idempotent `requestId` checked before the drop token bucket (4/s, burst 3), prizes only within 150 s of a drop (the client parks the motor 60 s after the last one and settles reloads with collection OFF — reloading can't shake coins loose), `lost` booked as gutter. Amulet raises RECYCLE 40→70% (mints nothing). **Physics: 120 Hz** (at 60 Hz a pile of 3 mm coins never stops rocking), `lengthUnit` 5 (cm world), every ordinary coin the SAME body (only looks differ, or the return would depend on the stake), turbo is UI-only. Run order: `coinpusher.sql`, **then** re-run `hazard-views.sql` + `coin-inflow-stats.sql` + `economy-stats.sql` + `last-active.sql` (stamps on `coinpusher_coins`, since the session rows are UPDATEd), then `supabase functions deploy coinpusher-action`. CSP needs `'wasm-unsafe-eval'` (Rapier). Full design, tuning evidence and measured RTP in docs/coinpusher.md.
- `supabase/marketplace.sql` — peer-to-peer marketplace ("Targowisko") tables/RPCs; any user can list IRL goods/services; winning/buy coins transfer from buyer to seller (not burned)
- `supabase/poker.sql` — poker tables, RLS, realtime publication, and leaderboard stack accounting
- `supabase/functions/poker-action` — authenticated Edge Function that owns poker state transitions, hidden cards, and chip accounting
- `supabase/plinko.sql` / `supabase/functions/plinko-action` — „Plinko G6" solo casino game; the Edge Function owns each drop's randomness, path, payout, and coin accounting, while the frontend only animates the trusted server result
- `supabase/mines.sql` / `supabase/functions/mines-action` — „Miny G6" casino game; clients read own sanitized round/history only, hidden mine tiles live in `mines_round_secrets`, and all reveals/cashouts are owned by the Edge Function
- `supabase/crash.sql` / `supabase/functions/crash-action` — „Rakieta" (Crash), a **solo** casino game: each player flies their own rocket (`crash_rounds`, hidden crash point in `crash_round_secrets`); the Edge Function (`start`/`state`/`cash_out`) owns the RNG, lazy bust-on-read resolve, and payout, with results in `crash_spins`. No shared table, no realtime — the client polls `state` on a freeze-proof clock, with a 3s client-side launch countdown before `start` commits coins. **Cash-out fairness contract (WYSIWYG):** `cash_out` pays the claimed `atMultiplier` even slightly above the hidden crash point, bounded by a `CASHOUT_GRACE_MS` (450ms) arrival window and a display-lead cap, so an honest click during the grace period never loses to a race with the bust reveal. **Parity contract:** `CRASH_GROWTH = Math.log(2)/15000` (doubles every 15s) must match byte-for-byte between `index.html` and the Edge Function. Full mechanism (bust-nudge realtime broadcast, exact EV math) is in docs/db-migrations.md.
- `supabase/wheel.sql` / `supabase/functions/wheel-action` — displayed as **„Koło Żubra G6"** 🦬 (a Żubrówka-themed reskin; every internal identifier stays `wheel`). A **shared-rounds** casino game: one communal wheel, 20 ordered segments, no risk tiers (RTP 95.0%). `wheel_rounds`/`wheel_round_bets` are public-SELECT/no-client-write, realtime-published, mirroring the roulette pattern. **Coin timing follows the ROULETTE convention:** `bet` only validates balance; coins move for every bet at once when the round resolves. Resolution is **lazy-on-read, crash-style** (`resolveDueRound` runs at the top of `state`/`bet`/`ready`) with a 15s betting window and a ready-vote fast-start. Lock order is round row then profile row throughout, so the paths can't deadlock. **Parity contract:** the ordered 20-element multiplier array (`WHEEL_SEGMENTS` in `index.html`, `SEGMENTS` in the Edge Function) must stay byte-for-byte identical. Gets the casino-luck amulet boost like the other house games. Run order: `wheel.sql`, then re-run `hazard-views.sql` + `coin-inflow-stats.sql` + `economy-stats.sql`. Full ceremony/animation design is in docs/db-migrations.md.
- `supabase/whack-boss.sql` — Whack-a-Boss rounds, weekly/all-time leaderboard views, and scheduled weekly prize payout
- `supabase/functions/whack-boss-action` — authenticated Edge Function that issues and validates 18-second Whack-a-Boss rounds
- `supabase/bug-jumper.sql` / `supabase/flappy-pants.sql` / `supabase/snake.sql` / `supabase/invoice-horde.sql` / `supabase/var-patrol.sql` / `supabase/egg-catch.sql` / `supabase/super-mariusz.sql` / `supabase/popup-panic.sql` / `supabase/tetris.sql` / `supabase/saper.sql` — seasonal arcade games (Bug Jumper, „3 Pary Spodni", Snake, „Najazd Ticketów" — internal gameType `invoice_horde`, VAR Patrol — internal gameType `var_patrol`, „Łap Jajka" — internal gameType `egg_catch`, „Super Mariusz" — internal gameType `super_mariusz`, „Zamknij Popupy!" — internal gameType `popup_panic`, „Tetris G6" — internal gameType `tetris`, „Saper Maraton" — internal gameType `saper`); each adds `*_rounds`/`*_scores`/`*_weekly_awards` tables, three leaderboard views, an `award_*_week()` payout, realtime publication, and a Monday `pg_cron` job after the week closes
- `supabase/bug-jumper-dynamic-course.sql` — run after `supabase/bug-jumper.sql`, idempotent. Adds a `seed` column to `bug_jumper_rounds` and repoints the leaderboard/award views + `award_bug_jumper_week` at `course_id = 'bug_jumper_dynamic_v1'`, powering the 2026-07-27 relaunch with a procedurally generated (seeded, per-round) course instead of the old fixed Hard Course — see the Seasonal games section below
- `supabase/functions/bug-jumper-action` / `supabase/functions/flappy-pants-action` / `supabase/functions/snake-action` / `supabase/functions/invoice-horde-action` / `supabase/functions/var-patrol-action` / `supabase/functions/egg-catch-action` / `supabase/functions/super-mariusz-action` / `supabase/functions/popup-panic-action` / `supabase/functions/tetris-action` / `supabase/functions/saper-action` — authenticated Edge Functions that issue and validate seasonal-game rounds
- `supabase/healer-dungeon.sql` / `supabase/functions/healer-dungeon-action` — „Uzdrowiciel G6" (internal gameType `healer_dungeon`) promoted from arcade-only to a full seasonal game (2026-08, debuts via override on the week starting 2026-08-03); same `*_rounds`/`*_scores`/`*_weekly_awards`/three-views/`award_*_week()`/cron shape as every other seasonal game, with two differences: the class is chosen **before** the round starts and stored on the round row (threaded through the replay like the seed), and the round has no natural "clean end" — it only stops on the healer's death or a replay-cost safety cap, both valid submissions. See the „Uzdrowiciel G6" section below and docs/healer-dungeon.md for the full sim and balance design.
- `supabase/filler.sql` / `supabase/functions/filler-action` — „Filler" (internal gameType `filler`), a **1v1 territory flood-fill** game, added 2026-08 as arcade-only via „Wszystkie Gry" (seasonal promotion deployed and armed but **scheduled for no week** — its 2026-08-10 slot was reassigned to „Kulki G6" on 2026-08-05). **Server-authoritative for every move** (Poker/Roulette/Wheel model, not the client-replay pattern every other arcade game uses) — a "vs bot" match is a 2-seat match where seat 1 is `(user_id NULL, is_bot true)`, so bot-mode and PvP (public queue + bot fallback) share one Edge Function/one pair of tables (`filler_matches`/`filler_match_players`), with no secrets table (the board has no hidden state) and no parity contract (no client-side authoritative sim to keep in sync). ⚠️ A legal color pick must exclude BOTH the caller's own and the opponent's current territory color, or one flood-fill move wins instantly by absorbing the opponent's whole territory. ⚠️ The board is a **DIAMOND LATTICE**, not a square grid (2026-08-04) — odd rows sit half a tile right and rows overlap by half, so a tile shares edges only with the two above and two below; `neighbors4()` in the Edge Function and the layout in `games/filler.js` are one decision in two places and **must be deployed together**. Board is **21×27=567 tiles, 7 colors** (renders 1.54:1 landscape, since h rows are only (h+1)/2 tall), with bot practice matches on a faster **15×19=285**; `width × height` must stay **odd**, or ties become reachable (majority draw, no `winner_seat`), so any future resize is constrained to odd dimensions. Starting corners are assigned **at random** per match — the lattice's corners have unequal degree, so a fixed assignment measured 43-45% for seat 0. "One open match per user" is enforced by a real partial-unique-index constraint (trigger-maintained), not just app logic. **Bot matches never write an `arcade_scores` row, win or lose** — only PvP results score — the only fix that closes a free/risk-free farming vector against the best-score-ever leaderboard, since `arcade.sql`'s RPCs (and their rate-limit) are deliberately bypassed. Full design in docs/filler.md.
- `supabase/bubble-breaker.sql` / `supabase/functions/bubble-breaker-action` — „Kulki G6" (internal gameType `bubble_breaker`) promoted from arcade-only to a full seasonal game (2026-08-05, debuts via override on the week starting **2026-08-10**); same `*_rounds`/`*_scores`/`*_weekly_awards`/three-views/`award_*_week()`/cron shape as every other seasonal game. Its replay is the cheapest in the rotation — no tick clock, a round is just `(seed, [tapped cell indices])` — which is also why the Edge Function carries a wall-clock `BB_MIN_MS_PER_MOVE` guard against offline solvers. `arcade.sql` needs no change: the free-arcade path is untouched and writes to `arcade_scores`, not `bubble_breaker_scores`. See the „Kulki G6" section below and docs/seasonal-games.md.
- `supabase/saper.sql` / `supabase/functions/saper-action` — „Saper Maraton" (internal gameType `saper`), a brand-new full seasonal game added 2026-08-22 and debuting via override on the week starting **2026-08-24**; same `*_rounds`/`*_scores`/`*_weekly_awards`/three-views/`award_*_week()`/cron shape as every other seasonal game. Minesweeper as a **90-second score chase** over a stream of boards on a five-rung ladder (6×6/5 mines → 9×9/16): clear one, the next deals instantly and harder; hit a mine and you lose the board, the streak and **5 s off the clock** — but not the run, which is the rule the whole design rests on (a marathon that ended on a forced 50/50 guess would be miserable). Every board's first click is guaranteed safe (mines placed after it, 3×3 exclusion), and **chording** — tapping a revealed number whose flags add up — is the affordance that separates a fast round from a slow one. **Anti-cheat is the clock, not a per-move minimum:** the round IS a tick counter and `submit` requires `elapsed_wall_clock >= replay.ticks × SP_TICK_MS`, so forging a full 1800-tick log costs 90 real seconds; measured against **ticks actually simulated**, not `SP_ROUND_TICKS`, since penalties end a round early. The client clock is wall-time-derived, so backgrounding the tab burns the round down instead of buying thinking time. **Parity contract:** the fenced `SP_*` PARITY BLOCK must stay byte-for-byte identical between `games/saper.js` and the Edge Function; `scripts/saper-parity.mjs` extracts and evaluates both from source rather than carrying a third copy, and `scripts/saper-balance.mjs` re-derives the score bands and asserts the first-click-safety and ladder invariants. Run order: `saper.sql`, then re-run `season-award-gating.sql` **and** `arcade.sql` (both updated: `saper` whitelisted and capped at 9999).
- `supabase/arkanoid.sql` / `supabase/functions/arkanoid-action` — „Arkanoid G6" (internal gameType `arkanoid`), a brand-new full seasonal game added 2026-09-22 and debuting via override on the week starting **2026-09-28**; same `*_rounds`/`*_scores`/`*_weekly_awards`/three-views/`award_*_week()`/cron shape as every other seasonal game. Breakout with office bricks: six ~50-brick floors that loop faster, three lives, capsules, a 5-minute replay ceiling. **Integer fixed-point sim with no trig** (bounces from the integer `AK_DIRS` table), so JavaScriptCore and Deno cannot disagree. **Input is a paddle TARGET** the paddle chases, logged as a flat `[tick, value, …]` array only on change (value −1 = launch); `AK_MAX_INPUTS` sits above `AK_MAX_TICKS` because a mouse moving every tick is honest play. Anti-cheat is Saper's clock rule (wall clock ≥ ticks × 20 ms); the client treats a long frame gap as a pause. **Parity contract:** the fenced `AK_*` PARITY BLOCK must stay byte-for-byte identical between `games/arkanoid.js` and the Edge Function — `scripts/arkanoid-parity.mjs` extracts both from source; `scripts/arkanoid-balance.mjs` asserts no-ball-trap, the score cap (40 000) and the skill spread. Its panel CSS is injected by the game file (payload budget). Run order: `arkanoid.sql`, then re-run `season-award-gating.sql`, `arcade.sql` **and** `last-active.sql`. Full tuning notes in docs/seasonal-games.md.
- `supabase/season-award-gating.sql` — idempotent; season-gates the weekly award cron jobs via `seasonal_game_for_week()` and schedules them for **Monday 00:00 Europe/Warsaw** with a dual-UTC-hour + local-hour gate that remains correct across DST; must be kept in sync with the rotation constants in `index.html` and re-run when they change
- `supabase/seasonal-prizes-x2.sql` — idempotent (CREATE OR REPLACE only); doubles every seasonal game's Monday payout to **🥇 1000 / 🥈 500 / 🥉 200** (2026-07-26). A convenience bundle carrying all ten `award_*_week()` functions with just the prize amounts changed — re-running it after editing one of those functions elsewhere will revert your edit unless you patch this file too. Already-awarded weeks are untouched.
- `supabase/polish-midnight-schedules.sql` — run last after garden watering, seasonal games/gating, and farm seasonal SQL. Production-safe consolidation migration: makes the Zen-garden daily watering allowance use the Warsaw calendar date, schedules all weekly seasonal/farm payouts for **Monday 00:00 Europe/Warsaw**, and aligns physical rotten-lot cleanup to Warsaw midnight; the dual UTC schedules are internally Warsaw-hour-gated for DST safety
- `supabase/fix-client-meta-double-encoding.sql` — idempotent; normalizes double-encoded `client_meta` on seasonal `*_scores` tables (one-time migration + BEFORE INSERT trigger), required for the leaderboard base/bonus score split
- `supabase/football.sql` — World Cup 2026 fixed-odds betting ("Mundial"); adds `football_matches`/`football_bets` tables (text event ids, SELECT-only for clients), extends the `leaderboard` view to count open bet stake, realtime publication, and an **every-6h** `pg_cron` job that `net.http_post`s the Edge Function to refresh odds and settle results. **Before running, replace the `__PROJECT_REF__`/`__FOOTBALL_CRON_SECRET__` placeholders in the cron block.**
- `supabase/functions/football-action` — Edge Function (`verify_jwt = false`) that owns all football writes: `state` (public read), `bet` (JWT-validated fixed-odds bet that locks server-side odds), and `cron` (`x-cron-secret`-gated sync+settle pulling events/odds/results from **The Odds API v4**)
- `supabase/economy-stats.sql` — **requires `farm-card-composter.sql` first** (`farm_card_stack_value()`); idempotent; adds `economy_stats()` SECURITY DEFINER aggregate RPC that returns server-wide coin supply breakdown (cash, market positions, football/poker/auction escrow, hero items, accessories) plus per-player `holdings` array and `richest` for the **Skarbiec G6** panel on the Statystyki tab
- `supabase/activity-stats.sql` — idempotent; adds `player_activity_stats()` SECURITY DEFINER RPC powering the **📅 Aktywność** card on Statystyki (last seen, active days, streaks, per-area event breakdown). Must be server-side since most source tables are own-row RLS. Areas: `casino`, `games`, `markets`, `football`, `canvas`, `farm`/`marketplace` (derived from `coin_transactions.reason` prefixes). Day bucketing is Europe/Warsaw; admins excluded; frontend degrades gracefully if the function is missing.
- `supabase/last-active.sql` — run after `activity-stats.sql` (it seeds its backfill from `player_activity_stats()`); idempotent. Adds `profiles.last_active_at` plus `touch_last_active()`, one generic AFTER INSERT trigger installed on **30** event tables (the same list `player_activity_stats()` UNIONs, **plus `hilo_spins`, `tower_spins`, `coinpusher_coins`, `saper_scores` and `arkanoid_scores`, which that RPC still misses**). Powers the side rail's „Biuro" list: the rail first read the RPC, which UNIONs ~25 tables and does a gaps-and-islands streak pass just to produce one timestamp per player, forcing a 5-minute refresh floor on a list that is on screen on every tab. Stamping on write makes it `select id,nick,last_active_at from profiles`. ⚠️ **The `FOR UPDATE SKIP LOCKED` in the trigger is the safety argument, not an optimisation** — it fires inside other people's transactions (every spin, trade and ledger row), several of which already hold locks on more than one `profiles` row (a marketplace sale locks buyer *and* seller), so an unconditional UPDATE would add a second, differently-ordered profiles lock: exactly the shape that deadlocked the farm award cron against the land-tax cron. SKIP LOCKED cannot wait, so it cannot deadlock, and a skipped stamp is harmless (the transaction holding the row is itself activity). Rows locked by the *calling* transaction are not skipped, so the normal `SELECT … FOR UPDATE`-then-insert RPC still stamps. The body is wrapped in `EXCEPTION WHEN OTHERS` on purpose: a cosmetic stamp must never roll back a payout. ⚠️ **`game_transactions` is a VIEW** (over the spin tables + `poker_ledger`, all already covered) and cannot carry a row trigger — the installer's `relkind` guard skips views with a NOTICE rather than failing. Re-run this file after any file that DROPs and recreates one of the 30 tables, since that takes its trigger with it. `player_activity_stats()` is unchanged and still owns the Statystyki card.
- `supabase/canvas.sql` — „Wspólne Płótno" collaborative pixel canvas; adds `canvas_pixels` (one shared 192×108 board, public SELECT) + `canvas_cooldowns` (own-row SELECT, now unused) tables, realtime publication on `canvas_pixels`, and the original `place_pixel(x,y,color)` SECURITY DEFINER RPC (its per-user cooldown is superseded — see `canvas-free-for-all.sql`). Idempotent (`CREATE TABLE IF NOT EXISTS` / `CREATE OR REPLACE`); the bounds (192×108) live in the RPC and mirror `CANVAS_W`/`CANVAS_H` in `index.html`
- `supabase/canvas-free-for-all.sql` — run after `canvas.sql`; idempotent (`CREATE OR REPLACE`). Redefines `place_pixel` to drop the 2h-free/pay-1-coin split — every pixel is now free for every user, always; `canvas_cooldowns` is left in the schema but no longer read or written
- `supabase/canvas-paint-log.sql` — run after `canvas-free-for-all.sql` + `lottery.sql`; idempotent. Adds `canvas_paint_log` (no client grants — written/read only by SECURITY DEFINER functions) and redefines `place_pixel` to insert one log row per paint, fixing the Loteria's "Wspólne Płótno" ticket count: `canvas_pixels` alone is last-write-wins, so a player's earlier painting days disappear once someone else paints over that pixel. `mundial_lottery_standings()`'s `canvas` CTE is redefined to union days from `canvas_pixels` (current-owner, the old lower-bound proxy) with `canvas_paint_log` (accurate going forward) so no one's existing tally regresses. Re-run after re-running either superseded file.
- `supabase/chat.sql` — „Czat G6" office chat: an ICQ-style rail present on **every tab** (public room + 1-on-1 whispers + heartbeat presence). Adds `chat_messages` (`recipient_id IS NULL` ⇒ public room, non-null ⇒ whisper) + `chat_presence` (`last_seen`/`online`), both public-SELECT/no-client-write and realtime-published. Uses the **direct SECURITY DEFINER RPC** pattern from `canvas.sql` (no Edge Function): `chat_send(p_recipient, p_body)` (trim + 1000-char cap + ≥1s rate-limit — the only anti-spam throttle, like `record_arcade_score`), `chat_heartbeat()` (marks caller online + sweeps stale sessions offline, the presence analog of roulette's `evictStaleSeats`; returns the online id list), `chat_set_offline()` (immediate offline flip on logout), `chat_mark_read(p_peer)`. **RLS is the whisper boundary** — a message is SELECT-able only if it's public or `auth.uid() IN (sender_id, recipient_id)`, enforced for realtime delivery too, so a whisper reaches only the two parties (and can be sent to an OFFLINE user — stored, shown when they return). The offline→online / online→offline flip on `chat_presence.online` is the realtime signal `index.html` turns into ICQ „wszedł/wyszedł z sieci" notices. A minute `pg_cron` job (`chat_presence_sweep`) closes the last-user-leaves gap the heartbeat can't. **Constants mirrored in `index.html`:** `CHAT_STALE` (90s) ↔ `CHAT_HEARTBEAT_MS` (25s, client ≤ ⅓ rule), `CHAT_MAX_LEN` (1000), the ≥1s send throttle. Idempotent; run once via `supabase db query --linked`. Frontend: `initChat`/`chatIngest`/`chatOnPresence`/`chatHeartbeatPing` + the `#chat-rail` markup, wired into `showApp`/`doLogout` and the shared `db-changes` realtime channel. ⚠️ Its `chat_heartbeat()`/`chat_set_offline()`/`chat_presence_sweep` are **superseded by `chat-presence-quiet.sql`** — re-run that after re-running this.
- `supabase/chat-presence-quiet.sql` — run after `chat.sql` (supersedes its `chat_heartbeat()`/`chat_set_offline()`/`chat_presence_sweep` cron; re-run after re-running that file); idempotent. Kills the presence heartbeat's realtime fanout (2026-08-07). `chat.sql` wrote `last_seen` on **every** 25s beat, and `chat_presence` is realtime-published with the client on `event:'*'` — so every beat was RLS-evaluated and broadcast to every connected client (**N users ⇒ N²/25 messages/sec**), all of it discarded on arrival since `chatOnPresence()` early-returns when `online` didn't change. Measured on prod: the realtime WAL query ran ~2×/s with nobody playing. The fix **splits the hot column out of the published table** — `last_seen` moves to `chat_presence_beats` (NOT published, no client grants, RLS-on-zero-policies), and `chat_presence` is written **only when `online` truly flips**, via a conditional `ON CONFLICT … DO UPDATE … WHERE chat_presence.online IS DISTINCT FROM true` (verified: 0 rows written while already online, 1 on a genuine flip/join). Steady-state WAL on the published table is now zero; realtime carries only real join/leave events. `chat_set_offline()` is likewise guarded so a double logout is a no-op. Sweeps use `COALESCE(beat.last_seen, presence.last_seen)` so pre-migration rows stay sweepable. **No frontend change** — the client still reads `chat_presence(user_id, online)`, subscribes to the same table, and `chat_heartbeat()` still returns the authoritative online list for `chatReconcileOnline()`. `chat_presence.last_seen` is deliberately kept as the fallback column.
- `supabase/chat-reactions.sql` — run after `chat.sql`; idempotent. **Reactions + animated „Naklejki" in Czat G6.** Adds `chat_reactions` (PK `message_id,user_id,code`, RLS mirrors the whisper boundary via an `EXISTS` on `chat_messages`, realtime-published) and `chat_toggle_reaction(p_message_id, p_code)` (code whitelist, visibility check, ≤3 codes per user per message, 300 ms per-user throttle). ⚠️ **Removing a reaction flips `active = false`; rows are never DELETEd** — realtime doesn't apply RLS to DELETE payloads, so a delete would broadcast whisper reactions to everyone. A **sticker message needs no SQL**: it is a plain `chat_send` whose body is exactly `:st:<code>:`. The code whitelist `chat_react_codes()` / `chat_react_max_per_message()` are mirrored by `CHAT_FX_EMOJI`/`CHAT_FX_STICKERS`/`CHAT_FX_MAX_PER_MSG` in `tabs/chat-reactions.js` — add a code in both. Stickers are our own inline-SVG + CSS-keyframe art (Clash-flavoured, no image files, no Supercell assets).
- `supabase/lottery-draw.sql` — the Loteria's **actual draw + payout**, plus the „🎰 Maszyna losująca" ceremony UI (2026-07-31). Run after `lottery.sql` + `canvas-paint-log.sql` + `lottery-fixes.sql`; idempotent. Calls (doesn't redefine) `mundial_lottery_standings()`. Adds `lottery_draws`/`lottery_draw_secrets` + three admin-gated RPCs: `lottery_draw_commit` → `lottery_draw_run` → `lottery_draw_cancel`. **Fairness model is COMMIT–REVEAL:** commit seals a secret seed and publishes only its hash; the draw mixes in public entropy (an operator `public_seed` + a `force` slider) so the operator visibly influences the outcome without being able to aim it. The draw is weighted-without-replacement over the frozen ticket snapshot; tiers 30/20/14/10/8/6% mirror `LOTTERY_TIERS`, plus a 12% dividend to every ticket holder. Payout mints via `coin_transactions` (`lottery_prize`/`lottery_dividend`), patched into the live `economy_stats()` `ledger_minted` list via a guarded `DO` block. ⚠️ **No dry-run/preview by design** — one commit = one sealed draw (`lottery_draws_single_open_idx` enforces it); to rehearse, commit a throwaway. **Parity contract:** `lottery_roll()`, snapshot ordering, and tier percentages are mirrored in `index.html` (`lotRollFromHex`/`lotVerifyDraw`), which re-derives every roll client-side and shows ✓/⚠. The **„Na żywo"/„🧪 Test"** admin toggle runs the full ceremony against real tickets but draws winners client-side via `lotTestDraw` and never calls the server — zero `sb.rpc` calls, nothing is paid. Full ceremony/animation/physics design (ball-drum simulation, reveal ordering, colour assignment) is in docs/db-migrations.md.

- `supabase/lottery-fixes.sql` — run **LAST** among the lottery ticket-counting files (after `lottery.sql`+`canvas-paint-log.sql`, re-run after re-running either); idempotent. Fixes two ticket-count bugs from a 2026-07-16 audit: **Targowisko overcount** (cancelled listings were credited — now filters `status='settled'`) and **Farma undercount** (sold/rotten lots erase history — now unions the immutable `coin_transactions` ledger). All other categories were audited correct.
- `supabase/farm.sql` — „Farma" (Phase 1): a shared **13×4** owned-tile grid + collectible plant-card economy, exposed as **🌱 Ogródek**. Adds `farm_tiles`/`farm_card_defs`/`farm_collection`/`farm_inventory`/`farm_market`/`farm_nft_instances`/`farm_user_state` plus RPCs: `buy_farm_tile` (escalating BURN price, doubling, capped at 50000), `claim_farm_starter` (one-time 3 free boxes + tile voucher), `buy_farm_lootbox`/`open_farm_lootbox` (100🪙, weighted draw of 3 distinct cards), `level_up_card`, `plant_crop`/`harvest_crop` (server-timed growth, harvested crops become per-lot inventory that **rots 5 days later**), `sell_crop_to_npc` (fluctuating „stalk market" price, FIFO by soonest expiry), `farm_rot_cleanup` (daily cron). Pricing rolls twice daily (00:00/12:00 Europe/Warsaw) to 30-100% of the catalog max, dips per sale, recovers ~12%/hr — see `farm-price-history.sql`. Idempotent; includes a one-time migration linking legacy `gardens` rows onto plant tiles. **`FARM_W`(13)/`FARM_H`(4) must mirror `index.html`; coin reasons `farm_tile_buy`/`farm_box_buy`/`card_levelup`/`farm_crop_sale` must stay in sync with `economy-stats.sql`.** Full gameplay/economy detail is in docs/farma.md.
- Farm land tax (**soft cap**): fair share is `ceil(normal_capacity / active_farm_participants)` tiles/player; excess is taxed `1000×excess²`/day + 10% daily debt interest. Only unpaid debt blocks buying more tiles; planting is never blocked. Autopaid from crop/marketplace proceeds, assessed daily at 00:00 Europe/Warsaw. Unpaid `land_tax_debt` is a net-worth liability (subtracted in `user_assets_value`/`user_net_worth_breakdown`/`economy_stats`).
- `supabase/farm-price-history.sql` — „Farma" fluctuating-price engine + per-crop NPC price history for the **📈 Ceny** tab. Adds `farm_price_history`, `roll_farm_prices()` (rolls each crop to 30-100% of base twice daily), `maybe_roll_farm_prices()` (hourly cron gated to 00:00/12:00 Warsaw), `snapshot_farm_prices()` (2h recovery snapshots). **The regime bands live only in `roll_farm_prices()`; the per-transaction sale math (drop up to 40% scaled by qty, 12%/hr recovery, 30%-of-base floor) is duplicated between `sell_crop_to_npc` (`farm.sql`) and the client preview `farmSellQuote` (`index.html`) — keep both in sync**, including the collector/seasonal-contract achievement multipliers.
- `supabase/farm-marketplace.sql` — „Farma" Phase 3 (run after `marketplace.sql`+`farm.sql`): lets players auction/sell plant cards, serialized NFTs, and empty non-migration farm territories to each other by extending the Targowisko engine with item linkage + ownership transfer + `farm_nft_transfers` provenance/price log; idempotent
- `supabase/nft-leveling-rework.sql` — moves NFT level from `farm_collection` onto each `farm_nft_instances` row; adds `level_up_nft(hero, fuel)` merge RPC (fuel is burned forever), instance-aware `plant_crop(p_x,p_y,p_species,p_instance_id)` + `harvest_crop` (clears `planted_instance_id`), NFT-skipping lootbox, and level-aware listings/views. Run after `farm.sql`+`farm-marketplace.sql`; idempotent
- `supabase/nft-merge-fixes.sql` — run **LAST** among the farm files (supersedes copies in `farm.sql`/`farm-marketplace.sql`/`nft-leveling-rework.sql`); idempotent. Fixes merge fallout: `farm_card_defs.minted_count` (monotonic minted-ever counter) now drives lootbox serials/edition supply/persona names (live-instance counts break once merges burn cards); `farm_nft_transfers.instance_id` is nullable `ON DELETE SET NULL` (was CASCADE, erasing provenance); `settle_marketplace_listing` voids (refunds) rather than raises forever when a farm-tile auction winner has unpaid land-tax debt; drops the ambiguous 3-arg `plant_crop` overload.
- `supabase/marketplace-requests.sql` — Targowisko „Zlecenia zakupu": buyer-initiated requests, the mirror of a normal listing. Two mechanisms: **(1) standing buy order** (`marketplace_buy_orders`/`create_buy_order`/`fill_buy_order`) — buyer sets a fixed price + max qty for one item kind (card species, NFT species, or tile); any seller holding a match can instantly fill part of it, no negotiation, **no escrow** (balance checked live in `fill_buy_order`). **(2) open request/RFQ** (`marketplace_requests`/`create_marketplace_request`/`create_request_offer`/`accept_request_offer`) — buyer posts free text, sellers each propose price+qty, buyer manually accepts one (direct transfer, no escrow, no auto-delivery). Both reuse the existing `marketplace_purchase`/`marketplace_sale` coin reasons, so `coin-inflow-stats.sql` needs no changes. Frontend: **📥 Zlecenia zakupu** Sklep section with 💰 stała cena / ❓ zapytania subtabs. Run after `marketplace.sql`+`farm.sql`+`farm-marketplace.sql`+`farm-card-bulk-listing.sql`+`nft-merge-fixes.sql`; idempotent.
- `supabase/farm-card-bulk-listing.sql` — run after `farm-marketplace.sql`+`nft-leveling-rework.sql`+`farm-plant-once-fix.sql` (supersedes their copies of the listing/delivery RPCs); idempotent. Lets a seller list **more than one** fungible plant card in one Targowisko listing: adds `marketplace_listings.qty` and a 6th arg on `create_farm_card_listing` (`p_qty`, reserves N free duplicates). NFTs/tiles stay single-unit.
- `supabase/farm-bulk-lootbox.sql` — run after the farm/NFT files above; idempotent. Adds `open_farm_lootboxes(p_qty)` (cap **20**, mirrors `FARM_BOX_BULK_OPEN_MAX`): locks `farm_user_state`, loops the canonical single-box `open_farm_lootbox()` in one transaction so anti-hoarding/voucher/starter rules still advance box-by-box. Frontend: shared qty picker (`farmQtyStepper` — stepper + slider + MAX; buy cap 50) and, from 2 boxes up, an „Otwórz wszystkie" shortcut. `farmOpenBoxes(btn, qty, gold)` is the one open path for both box types (`openFarmLootbox`/`openFarmGoldbox` are thin aliases): it slices a batch bigger than the server's 20-box cap into consecutive calls and reveals the collected packs as ONE animation — `playFarmPackOpening` (1 box), `playFarmMultiPackOpening` (2–10, box-by-box ceremony), or its rapid streaming mode above `FARM_PACK_FAST_PACKS` (10), which fits any batch into ~3.5s. ⚠️ Never animate `transform` on `.farm-pack-card` — it is the flip container, and an animated transform there flattens the 3D context in Chrome so a face-up card shows its back for the whole animation (the landing punch lives on `.farm-pack-glow` for exactly this reason). See docs/farma.md.
- `supabase/farm-goldbox.sql` — run after `farm.sql`+`nft-merge-fixes.sql`; idempotent. Adds the second, premium, **fully self-contained** lootbox **„Złota Skrzynia" ⭐** (never touches the standard box's columns/RPCs): `boxes_gold` counter, `buy_farm_goldbox` (BURN 500, cap 50), `open_farm_goldbox`/`open_farm_goldboxes`. A copy of the standard draw with 7 deltas: 5 draws not 3, voucher base 0.15, flat NFT weight, a guaranteed rare+ floor on the first draw, no starter-gift logic. Worth 500 in net worth. **⚠️ Superseded by `farm-goldbox-no-sale.sql` below.**
- `supabase/farm-goldbox-no-sale.sql` — run after `farm-goldbox.sql` (supersedes its `buy_farm_goldbox`+grant; re-run this after re-running that). Idempotent. **The ⭐ „Złota Skrzynia" is WITHDRAWN FROM SALE (2026-07-27)** — `buy_farm_goldbox` now always raises `goldbox_not_for_sale`, EXECUTE grant revoked. Everything else is intact: owned boxes still open, still worth 500 in net worth, `farm_goldbox_buy` stays in ledger reason lists. Frontend has no buy UI; `farmGoldBoxCardEl()` renders only if the viewer still holds sealed gold boxes.
- `supabase/farm-card-composter.sql` — **„♻️ Kompostownik"**: the sink for surplus low-tier plant cards. Run after `farm.sql` + `farm-goldbox.sql` + `farm-card-bulk-listing.sql`; idempotent, supersedes nothing and is superseded by nothing (so re-running any other farm file can't revert it). **Duplicates were a one-way pile**: `level_up_card` costs `2×L` cards but `50×L²` coins, so the coin term becomes the binding constraint around level 6-8 and cards accumulate forever — measured 2026-08-30 on prod, **25 619 fungible cards office-wide** (16 891 common / 7 166 rare / 1 562 epic) held by players sitting on 59–46 000 coins, i.e. 930 420 🪙 of net worth in cards nobody could spend (a common duplicate is valued at a flat 20 🪙, uncapped). Adds `compost_cards(p_species, p_times)` — a same-species trade-up ladder, **15 common → 1 random rare · 10 rare → 1 random epic · 10 epic → 1 ⭐ Złota Skrzynia** — plus `farm_compost_log` (own-row SELECT) and three one-line `IMMUTABLE` policy functions (`farm_compost_cost`/`farm_compost_yield`/`farm_compost_max_batch`). ⚠️ **The rates are deliberately far worse than a box's natural rarity mix** (≈64/26/9%, so the "fair" common→rare rate would be ≈2.4:1): this is a junk sink, not a second economy — every rung burns net worth and strictly reduces the card count. ⚠️ **No coin cost, on purpose** — the players holding the biggest piles are the coin-poorest, so a fee would price out exactly the people it exists for; no branch touches `profiles.coins`, so it is neutral for docs/anti-inflation.md. ⚠️ **Two copies are never burnable**, mirroring `create_farm_card_listing`: one per tile currently planted with that species, and the **last** copy (a card is a permanent blueprint — composting your only Marchewka would silently delete the ability to plant it). Copies reserved by an open Targowisko listing are already subtracted from `farm_collection.count`. **Also caps the net-worth valuation of duplicates**, which was the same pile inflating the leaderboard: `farm_card_stack_value(count, level, rarity)` values the copies a player can actually spend on the next level-up (`2×level+1`) at face value (20/50/150) and the surplus at the composter's scrap rate, **derived from the compost costs** so the two halves can't disagree (common 50/15=3, rare 150/10=15, epic 500/10=50). Measured effect on prod 2026-08-30: card valuation 930 420 → **289 414 🪙**; players holding a handful of cards lose nothing, the top 3 of the Net Worth ranking are unchanged, but Ilo (5 085 cards / 495 coins) drops 4th→6th and Mariusz (157k cash, 23 cards) rises 7th→4th. ⚠️ **`leaderboard-net-worth-items.sql` and `economy-stats.sql` now call it and will not create without this file — run this one first**, and never re-inline the old flat `CASE d.rarity …` into any of the three net-worth functions. Client mirrors in `index.html`: `FARM_COMPOST_COST`/`FARM_COMPOST_YIELD`/`FARM_COMPOST_MAX_BATCH`, `farmCompostInfo()`, `openFarmCompostModal()` — preview only. UI is the **♻️ Kompostownik** button on the 🃏 Karty roślin group header in 🎒 Mój Majątek, where 💎 Karty NFT has 🧬 Skrzyżuj. Full rationale in docs/farma.md.
- `supabase/farm-static-nft-odds.sql` — run **LAST** among the farm/lootbox files (copies the live draw logic from `nft-merge-fixes.sql`+`farm-goldbox.sql`; re-run after re-running either); idempotent. Two changes (2026-07-17): **(1) Static NFT odds** — removes the anti-hoarding penalty (drop chance no longer scales down with owned NFTs); non-NFT `draw_weight`s are scaled up to compensate, tuned by bisection so a standard box lands at a headline **1.0% ANY NFT** (gold box 2.52%). ⚠️ This 1.0% is a property of a full 3-edition activation window, not a constant — it degrades as editions sell out. Percentages are never hardcoded in the UI; `farmAnyNftChancePct`/`farmCardBoxChancePct` compute them live from the weights. **(2) Opening log** — adds `farm_lootbox_opens` (public SELECT, no client write) written by both open functions, powering the **„📊 Statystyki skrzynek i fart"** popup (opens, NFTs pulled, expected NFTs, a luck factor). Any future weight change must stamp `nft_p` onto opens that predate it or the luck stats silently rescore against the new rate.
- `supabase/farm-lootbox-backfill.sql` — one-time idempotent backfill of historical lootbox opens into `farm_lootbox_opens` (run after `farm-static-nft-odds.sql`), since opens were never logged before that counter shipped. Reconstructs per-player standard/gold opens from buy totals minus current sealed boxes, and NFT hits from `farm_nft_transfers` mint rows. Adds `backfilled` + `nft_p` (old-era expected probability, since these opens predate the current static-odds rate) columns so the luck-factor stats stay honest. Estimates, not an exact ledger.
- `supabase/farm-hybrid-income-parity.sql` — run after `farm-nft-breeding.sql` (supersedes its `farm_hybrid_stats`/`breed_nft` copies); idempotent. Fixes the „hybryda lepsza od obu rodziców" promise, which was false when measured in **coins/day** (hybrids all harvest the same cheap shared crop, so growing more cheap units could still earn less than a premium parent). Synergy is now measured in coins/day: `yield = ceil(max(parentIncomePerDay) × 1.15 × days / hybridCropPrice)`, with unit- and species-floor guards. **Hybrids also inherit `max(parentLevels)`** rather than minting at level 1 (fixes a net-worth regression), with level backed out of the stored stats so it isn't double-counted downstream. Adds `farm_nft_instances.stat_value` (net-worth value per level) so a bred hybrid's value is never below the better parent's — **patches the three net-worth functions (`user_assets_value`/`user_net_worth_breakdown`/`economy_stats`) in place via a guarded `DO` block** rather than re-transcribing them (they're large and deployed copies drift; it RAISEs if the expression isn't found). `farm_hybrid_stats` gains a 3rd arg (`p_hybrid`); the 2-arg overload is DROPped. **The JS mirror in `index.html` (`farmNftStats`/`farmBreedPreview`) must stay byte-for-byte equivalent.** Existing hybrids are not retro-fitted.
- `supabase/farm-nft-series-window.sql` — run after `farm-weekly-nft-series.sql` (supersedes its `farm_activate_weekly_nft()` + schedule view); idempotent. **Weekly NFT editions activate `farm_nft_series_lead_weeks()` = 2 weeks EARLY**, so the lootbox pool always holds a rolling window of 3 editions instead of 1 — fixes editions selling out mid-week and boxes silently having 0% NFT chance for days. Draw functions are unchanged (they already filter on `is_active`/`minted_count < edition_size`); this only widens the activation horizon. Frontend mirrors `FARM_NFT_SERIES_LEAD_WEEKS` (2) and shows a drought banner if the pool ever empties.
- `supabase/farm-seasonal-contracts.sql` — run after the farm/NFT files above. Adds weekly farm crop contracts starting **2026-07-06** with Marchewka, dynamic fair-cap-based bar targets, per-unit contract premium, top-3 payouts (2500+NFT / 1500+5 boxes / 1000+2 boxes), contributor bar payout (5 boxes for anyone with >=1 eligible unit), views for the 🏆 Wyzwanie hub, a **Monday 00:00 Europe/Warsaw** award cron, and a `sell_crop_to_npc` override that records eligible current-week harvested lots. Keep its sale-return JSON + event premium preview in `index.html` (`farmSellQuote`/`farmSeasonalEligibleQty`) aligned. ⚠️ Its award cron block is **superseded by `farm-seasonal-award-reliability.sql`**.
- `supabase/farm-seasonal-award-reliability.sql` — run after `farm.sql`+`farm-seasonal-contracts.sql`+`polish-midnight-schedules.sql`, and **re-run after re-running any of those three** (all carry ⚠️ notes); idempotent. Makes the 🏆 Wyzwanie Monday payout actually happen — it silently paid nobody for weeks **2026-07-13** and **2026-07-27**. Three faults: the award cron and the land-tax cron fired in the **same second** and both write `farm_user_state` in different row orders (deadlock); the job's two UTC hours are a **DST gate, not a retry**, so a midnight failure is final; and the retry wrapper someone had already written existed **only in the live DB**, so re-running the repo SQL reverted it — an ad-hoc `CREATE OR REPLACE` isn't a fix until it lands in `supabase/`. This file commits `award_farm_seasonal_week_cron()` (gate + 5× deadlock retry) and repoints the job at it, **moves the land-tax jobs to minute 10** (still Warsaw hour 00, tax day unchanged) to remove the root cause, and adds an **hourly** `farm_seasonal_awards_catchup` at minute **25** (clear of every other farm job) running `award_farm_seasonal_week_catchup()` — no hour gate on purpose, pays any closed unpaid week ≥2026-07-06 with sales, 4-week bounded lookback. See docs/db-migrations.md for the full post-mortem.
- `supabase/documents.sql` — „Moje Dokumenty": in-game document-signing archive. Adds `document_signatures` (own-row, stores a drawn-signature PNG) + idempotent `sign_document(doc_id, version, signature)` RPC. Document text lives in `index.html` (`DOCUMENTS` constant); the DB stores only the signature. A `gate:'farm'` mandatory doc blocks Ogródek open for all users until scrolled+signed, re-appearing on a `version` bump.
- `supabase/sign-board.sql` — „Tablica ogłoszeń": a communal wooden sign in the Ogródek sky, framed in the UI as a **rental** (internal RPC/table names still say `buy_*`). Idempotent. Adds `sign_board_messages` (public SELECT) + `buy_sign_board(message, days)` RPC (burns 100🪙/day, 1-7 days, one-owner-at-a-time via advisory lock). Message is trimmed, ≤80 chars, **immutable after publication**. Constants `SIGN_BOARD_PRICE`(100)/`SIGN_BOARD_MAX_DAYS`(7)/`SIGN_BOARD_MAX_LEN`(80) live only in the SQL. ⚠️ **The frontend for this was removed in `1a9aed7` („Ogródek: remove the »Wynajmij tablicę« sign-board rental entirely")** — there is no `sign_board` reference anywhere in `index.html`/`tabs/` any more, so nothing mirrors those constants and nothing can call `buy_sign_board`, even though the RPC is still granted to `authenticated` on prod. It was rented exactly once, for one day, on 2026-07-23. Re-adding a UI means re-adding the constants; otherwise consider revoking the grant.
- `supabase/remove-heroes.sql` — idempotent; **the cosmetic heroes system is REMOVED** (2026-07): drops `user_heroes` + `save_hero()` + its realtime publication, and flips tavern-only cosmetic items to `sale_type='auction'` (owners keep them; no longer sold in the shop). **Hero ITEMS stay fully functional**, now renamed **„Przedmioty specjalne"** in the UI: `hero_item_defs`/`hero_item_instances`/`hero_item_auctions`/`hero_item_auction_bids`, the purchase/bid/settle RPCs, the `hero_item_auction_cards` view, and the `hero_items` valuation in `economy-stats.sql`/`leaderboard-net-worth-items.sql` are all untouched. Bought in the Sklep (`#hero-shop-section`, `renderHeroShopGrid`) or won at 🔨 auctions (`#hero-auctions-section`); owned items list in the Ogródek hub's 🎒 Mój Majątek tab (`buildSpecialItemsSection` inside `buildFarmInventoryBody`). Run AFTER deploying the frontend that removed the ⚔️ Herosi tab.
- `supabase/hero-items-always-active.sql` — idempotent; run after `hero-items.sql`+`remove-heroes.sql`. **Items no longer need equipping — owning one is enough** (2026-07). Drops `hero_equipment`+equip/unequip RPCs, repoints every consumer at ownership directly: `my_hero_inventory`, `hero_score_bonus`, `award_daily_interest()`, and all 10 Edge Functions' shared `getStrongestHeroEffect(tx, userId, game)` pattern (query `hero_item_instances` by `owner_id`, no `hero_equipment` join).
- `supabase/casino-luck-item.sql` — idempotent; run after `hero-items.sql`+`hero-items-always-active.sql` (supersedes their `purchase_hero_item`/`my_hero_inventory`/`hero_score_bonus` copies). Adds **timed shop items** (`duration_hours`/`expires_at`) and the first one: **„Amulet Bezwstydnego Fartu" 🍀** (10000🪙, 7 days) — a **COMMUNAL** buff: while ANY unexpired instance exists (any owner), EVERY player gets a boost, applied server-side in 5 Edge Functions (slots +2 weight, roulette +1% on wins, plinko ×1.01, mines house factor 0.95→0.98, crash cash-out ×1.03) — deliberately overpowered, can push RTP above 100%. **Roulette rescue rework** (same file): the old `lucky_trousers` rescue paid full multipliers (+32% EV exploit); now pays a stake refund capped server-side at 1% trigger chance. Each purchase **queues +7 days onto the shared window** (not a personal timer). A daily cron deletes expired instances (also removes them from net worth). Only poker is excluded (PvP).
- `supabase/hide-legacy-hero-items.sql` — idempotent; run after `hero-items.sql`. Flips `sale_type` to `'hidden'` for six early low-impact items (`dealer_hat`, `luck_brooch`, `reflex_gloves`, `jumper_boots`, `bluff_dagger`, `lucky_trousers`) so they drop out of `renderHeroShopGrid()`'s catalog query (`.in('sale_type', ['shop','both'])`) without deactivating them — `is_active` stays `true`, so existing owners keep the effect and still see the item in `my_hero_inventory`/Mój Majątek (own-row, no `sale_type` filter). `hero-items.sql`'s own copies of these six rows are also flipped to `'hidden'` (⚠️ comment added) so a future re-run doesn't resurrect them in the shop.
- `supabase/lottery.sql` — „Loteria po Mundialu": an activity-based raffle on its own nav tab (`#tab-lottery`). Adds the read-only `mundial_lottery_standings()` RPC returning the live prize pool + per-player ticket standings. **Prize pool = 100× the Bank's net profit from settled Mundial bets** (⚠️ was 10× until 2026-07-31 — the multiplier is duplicated across three files, see docs/db-migrations.md for which ones must move together). Tickets are earned across the whole portal with per-source caps (Mundial bets, market trades, casino play, seasonal scores, farm activity, marketplace trades, canvas painting), plus ownership rewards (Zen garden, Ozdoby, Działki) and a breadth bonus for touching many activity categories. Admins excluded; all day-bucketing is Europe/Warsaw. The actual weighted draw/payout lives in `lottery-draw.sql` — this file ships only the live standings + rules panel. Frontend: `renderLottery()`, a **Lista/Macierz** ranking toggle, and a personal ticket breakdown pinned at the bottom.
- `supabase/prod-hardening.sql` — idempotent; adds indexes and tightens permissions; safe to re-run
- `supabase/reset-data.sql` — wipes all markets, trades, and poker state, resets every profile to 1000 coins

## Architecture

The app shell — HTML structure, all CSS, and the shared JavaScript — lives in `index.html`. No bundler, no framework, no TypeScript. **Feature code does not live there**: seasonal games are in `games/*.js` and whole tabs are in `tabs/*.js`, each fetched the first time it is needed.

**Payload budget (enforced).** `index.html` grew 241 KB → 1.88 MB between May and August 2026 with no single commit looking unreasonable, until a mid-range phone needed seconds of parsing before first paint. CI now fails the deploy if `index.html` exceeds **1600 KB** (`.github/workflows/pages.yml`). Adding a feature means adding a `tabs/*.js` module, not another 40 KB inline. If something genuinely belongs inline, raise the budget in the same commit so it is a reviewed decision.

**Lazy tab modules.** `TAB_MODULES` maps a tab to its `tabs/*.js`; `ensureTabModule(tab)` fetches it once (promise-cached) and `withTabModule(tab, fn)` runs the entry point once it is there. Currently split out: `stats.js` (Statystyki/Skarbiec/coin race), `lottery.js`, `plinko.js` (Plinko **and** Koło Żubra), `shop.js` (Sklep/Targowisko/Zlecenia), `poker.js`, `bank.js` (Bank G6), `hilo.js` (Drabina Kariery), `tower.js` (Wieżowiec G6), `coinpusher.js` (Automat Monet G6 — also `import()`s three.js + Rapier from jsDelivr itself and loads `games/coinpusher-core.js`). The scope rules are the same ones `games/*.js` follows:
- a module owns its top-level `const`/`let` — index.html must **not** also declare them (redeclaration across classic scripts is a hard error)
- its `function` declarations overwrite the no-op stubs index.html keeps for teardown calls `doLogout()`/`switchTab()` make unconditionally
- it reads shared globals from index.html, which always runs first
- anything called **synchronously** from outside the tab (the farm hub, `showApp()`, `setupRealtime()`) must be hoisted into index.html instead — see the „Shared with tabs outside Sklep" block. Anything called from a **click handler** can just be wrapped in `withTabModule()`.

`scripts/extract-tab.py` performs the move and enforces those rules: it refuses a region that redeclares a name, leaks a `const` outside, or has top-level side effects (`--allow-side-effects` opts in when they are only listener wiring).

**On-demand libraries.** `ensureLib(name)` loads `lightweight-charts` (charts, Rynki only) and Turnstile (only if `CAPTCHA_ON` is ever switched back on). Only `supabase-js` is required to boot — the inline script builds `sb` at top level, so it cannot be deferred; it is preloaded in `<head>` and executed at the end of `<body>`.

**Mobile chrome.** Below 900px the header, nav bar and chat rail drop `backdrop-filter` and go opaque (last rule in the stylesheet, so it beats the later `.chat-*` definitions). They are sticky and on screen on every tab, so the blur was being recomposited on every scroll frame. `isolation: isolate` preserves the stacking context the chrome z-order depends on (popovers 30 < nav 40 < header 41).

**Supabase client** is loaded from CDN. The anon key is intentionally public — Supabase RLS and function grants are the security boundary.

### Key global state

| Variable | Contents |
|---|---|
| `me` | Logged-in user profile (`id`, `nick`, `coins`) |
| `markets[]` | All market rows from DB |
| `tradesByMarket{}` | All individual trade rows, keyed by `market_id` |
| `myPositionsByMarket{}` | Current user's aggregated positions (from `positions` view) |
| `pokerState` | Sanitized shared poker table state returned by the Edge Function |
| `whackBossRuntime` | Current local Whack-a-Boss round timing, clicks, and score |

### Data flow

1. `loadMarkets()` fetches markets, trades, and profiles in parallel then calls `renderMarkets()`.
2. `renderMarkets()` fetches the current user's positions (`positions` view), then calls `buildMarketCard()` for each market.
3. Realtime subscriptions on `markets` and `trades` tables update state and call targeted update helpers (`updateCardProb`, `updateCardChart`) instead of full re-renders where possible.
4. **Those handlers only touch the DOM while the Rynki tab is showing** (2026-08-07). `renderMarkets()` costs a `positions` round-trip plus a rebuild of every card (each with its own lightweight-charts instance), and it used to run on **every trade by anyone, on every client, on every tab** — so a busy market made the farm/casino tabs janky for people not even looking at it. The in-memory state (`markets[]`, `tradesByMarket`) is still updated eagerly on every event so the grid is correct the instant it is shown; only the render is deferred, via a `marketsDirty` flag that `switchTab('markets')` flushes with `flushMarketsIfDirty()`. **Keep that invariant when adding handlers: update state unconditionally, gate rendering on `activeTab`.** A market *resolution* still calls `refreshMeCoins()` unconditionally — that one really does move everyone's balance.
5. Likewise `football_bets` only calls `refreshMeCoins()` when `payload.new.user_id === me.id`. That table is `SELECT`-able by everyone (`USING (true)`), so realtime hands every client every player's bets, and only your own row can move your own balance.

### Auth

Nick + 5-digit PIN. Implemented as Supabase email/password auth using the synthetic email `nick@typuje.local`. A Postgres trigger (`handle_new_user`) creates the `profiles` row on first signup. Economy writes go through RPCs or Edge Functions; the admin-only garden stage/streak controls use column-limited direct updates protected by RLS.

### CPMM pricing

`p(YES) = no_shares / (yes_shares + no_shares)`. Markets start at `yes_shares = no_shares = 500` (50%). Constant product `k = yes_shares × no_shares` is maintained by `place_bet`. Shares out for a NO bet of X coins:

```
dN = (N × X) / (Y + X)
sharesOut = X + dN
```

### Payout formula

Resolution distributes the total pot via the RPC `resolve_market`. The formula (mirrored in `estimatePayout()` for the live UI estimate):

```
payout = user_amount_bet + losing_side_pot × (user_shares / total_winning_shares)
```

Winners always get at least their bet back; the losing side's coins are split proportionally by share count. The estimate and the actual SQL resolution use identical math.

### DOM helper

`el(tag, attrs, ...children)` creates DOM nodes. **Children must be strings or Node objects — never arrays.** When you need to mix text and multiple element nodes outside of `el()`, use `node.append(str, node, str, ...)` instead.

### Permissions

All mutations require authentication and go through Supabase RPCs:
- `place_bet(market_uuid, side, amount)` — enforces coin balance, side-locking (can't bet both sides on the same market), and CPMM math atomically
- `create_market(icon, title, deadline)`
- `resolve_market(market_uuid, resolution)` — only market creator or a profile with `is_admin = true` can call this

Users can only add to one side per market (side-locked after first bet). Profiles with `is_admin = true` can resolve any market.

### Adding anything that holds value or moves coins — the four consumers

A new asset or coin-reason is never done when the SQL is done. Four frontend
consumers each carry their own hand-maintained list, and every one of them fails
**silently** — no error, just a wrong number or a missing row:

1. **`renderNetWorthBreakdown()`** (`tabs/stats.js`) — a hardcoded `parts` array.
   `user_net_worth_breakdown()` counts your key in `total` regardless, so
   forgetting the row makes 💼 Portfel's rows visibly not add up to 💎 Net Worth.
2. **`buildBalancePoints()`'s `NEUTRAL` map** (`tabs/stats.js`) — the harder one.
   A reason in `NEUTRAL` moves coins between cash and assets (net worth flat);
   anything absent is treated as pure income or loss. Escrow-shaped reasons
   (deposit/redeem, buy-an-asset) MUST be listed or the balance chart shows net
   worth cratering when a player deposits and spiking when it matures. Yields
   must NOT be listed — they are real income.
3. **`buildBalancePoints()`'s label chain** — unknown reasons fall through to
   `'🪙 ' + reason` and render the raw slug at the user.
4. **`ACT_COIN_REASONS`** (`index.html`) — an allow-list, so unknown reasons are
   dropped from the activity feed entirely. Per-player daily payouts belong out
   of it on purpose (see the `daily_interest` note there).

Bank G6 shipped missing all four and it took a direct "check the wallet" to find
it. Check them together.

### Bank G6 (investment products)

The `🏦 Bank G6` nav tab (`#tab-bank`) is where coins earn instead of sitting
still: term deposits, a piggy bank, tradeable bonds, and casino revenue shares.
Backend is `supabase/bank.sql` (see the file list above), frontend is entirely
`tabs/bank.js` — `index.html` holds only the shell div, the nav button, the
`.bk-*` CSS, and the `stopBankTimer()` teardown stub.

**The UI is deliberately not the app's house style.** A bank statement is a
reference document you read a column of numbers off, so the tab is built as one:
a navy statement header with a KPI strip, a sub-tab bar, and dense tables with
tabular figures and right-aligned money columns — closer to Business Central
than to the rest of the portal. Every product page follows the same order:
a four-cell „Warunki produktu" strip, the order form, then the positions table.
There is a published `Tabela oprocentowania` comparing every product on one
screen (it reads its caps from the server payload, not the client constants) and
a `Regulamin` section that carries all the prose. Conventions are in
docs/farm-ui-conventions.md's sibling, docs/bank.md.

**Limits are recomputed daily from measured economy data** (supply, the whole game's net minting over 30 days, active player count) and frozen for the day in `bank_limits`, with the full derivation and a week of history shown to players in a „Limity i budżet" tab — the Bank throttles itself when the game is already inflating. Rates are fixed and stored per position, so limit changes are never retroactive. **Where the inflation is:** the four bank products together mint ~1 250
coins/day at absolute maximum, against a ~2 951/day casino burn — the Sygnet
Bankiera, uncapped at 2%/day of cash by design, is ~7 215/day and compounding.
It is the only unbounded term in the feature, so it is the only one worth
retuning if the economy runs hot; `interest_cap` on that one row is the knob.

Two non-obvious mechanics: casino-share dividends pay off a **trailing-7-day
average**, not yesterday's net, because gambling here is bursty enough (25 998
on one day, ~0 on the surrounding six) that a single-day basis reads as a broken
product; and the client's `BANK_*` constants exist **only to preview** a number
before you commit — the server is authoritative for everything that moves coins.

### Marketplace (Targowisko)

The Sklep tab contains a peer-to-peer marketplace ("Targowisko 🛍️") where **any logged-in user** can list IRL goods or services for coins. Unlike the admin-only store rewards and hero-item auctions (where coins are burned), marketplace coins **transfer from buyer to seller**. Two listing formats are supported: fixed-price (`listing_type='fixed'`, instant buy) and timed auction (`listing_type='auction'`, bidding with escrow). The bidding engine mirrors `place_hero_item_bid`: coins are escrowed on each bid, the previous leader is auto-refunded when outbid, and the leading bidder only pays the incremental difference to top up their own bid. Settlement (`settle_marketplace_listing`) credits the escrowed winning amount to `seller_id`. Sellers (or admin) can settle their own auction; sellers can cancel an open listing with no bids. All RPCs are in `supabase/marketplace.sql`; the `marketplace_cards` view is used for card rendering.

### Poker

Poker is one shared authenticated Texas Hold'em table. Browser clients call `sb.functions.invoke('poker-action', ...)`; they do not write poker tables directly. The Edge Function verifies the user JWT, uses `SUPABASE_DB_URL` for a Postgres transaction, and stores hidden cards/deck in service-only tables. Public realtime updates on `poker_tables`, `poker_seats`, and `poker_events` only trigger a sanitized state reload.

Table defaults are 100 coin buy-in, 1/2 blinds, 6 seats, and a 30 second action timer. Sitting deducts the buy-in from `profiles.coins`; standing is allowed only between hands and returns the remaining stack. The leaderboard view includes active poker stacks so seated players keep their net worth while playing.

### Roulette (Ruletka)

Roulette is one shared authenticated table (`supabase/roulette.sql` + the `roulette-action` Edge Function; `verify_jwt` off, JWT checked per action). Up to 6 players sit in `roulette_seats`; coins are deducted **only at spin/resolve time** (`resolveRound`), never at bet time — so dropping a seat's open bets refunds nothing. The frontend (`#tab-roulette` → `loadRouletteState`/`renderRoulette*`) is a CSS-grid rounded-rect felt: a control dock on top, wheel + number board side-by-side, an on-felt seat strip, and a lower band holding the bets list + recent results. The unified button system is `.rl-btn` (`is-primary`/`is-ghost`/`is-gold`); the round chip selector reuses the shared `.casino-chip` (do not edit that base for roulette-only tweaks — scope under `.roulette-chips`).

**Idle-seat auto-eviction:** abandoned (closed-tab) seats are freed poker-style — `getState` runs `evictStaleSeats` on every read, deleting any seat whose `last_seen` is older than `SEAT_STALE_MS` (90s, server-only constant in `roulette-action`) and bumping the caller's own `last_seen`. The browser pings `state` every `ROULETTE_HEARTBEAT_MS` (25s, in `index.html`) while on the tab + visible + seated, plus on `visibilitychange`. The two constants are coupled: keep the client heartbeat ≤ ⅓ of the server threshold. `last_seen` lives on `roulette_seats` (idempotent `ADD COLUMN IF NOT EXISTS`).

### Plinko G6

Plinko G6 is a solo authenticated house game (`supabase/plinko.sql` + `plinko-action`). The browser calls the Edge Function directly with the active Supabase access token for `state`, `drop`, and queued `drop_batch` requests; it never writes tables directly and never chooses the path. The Edge Function validates stake/rows/risk, locks `profiles.coins`, generates crypto-random left/right paths, writes `plinko_spins`, updates the balance, and returns completed drops. The frontend (`#tab-plinko`) is an Office Arcade canvas board: glowing pegs, G6 ball trail, bucket bursts, paper/calculator/coffee particles, reduced-motion fallbacks, top-positioned start button, timed requests, and rapid-click queue playback. The payout tables live in both `index.html` (`PLINKO_PAYOUTS`) and `plinko-action`; keep them in sync, and re-run `hazard-views.sql`, `coin-inflow-stats.sql`, and `economy-stats.sql` after installing.

### Miny G6

Miny G6 is a single-player authenticated casino game (`supabase/mines.sql` + `mines-action`). The browser calls `sb.functions.invoke('mines-action', ...)` for `state`, `start`, `reveal`, and `cash_out`; it never writes tables directly and never receives mine positions while a round is active. A player can have only one active `mines_rounds` row; `mines_round_secrets` stores the hidden mine tiles with no client grants; completed rounds are copied to `mines_spins` for history, Hazardista, coin-inflow, and economy house-net stats. The 5×5 board size, 95% house factor, and multiplier cap live in both `index.html` and `mines-action`; keep them in sync.

### Whack-a-Boss

Whack-a-Boss is an 18-second authenticated mini-game. The browser calls `sb.functions.invoke('whack-boss-action', ...)`; the Edge Function creates the round schedule, validates submitted click timing/positions, stores scores, and returns weekly/all-time leaderboards. Weekly awards are paid by `award_whack_boss_week()` through `pg_cron`: rank 1 gets 1000 coins, rank 2 gets 500, rank 3 gets 200 (doubled 2026-07-26 — see `supabase/seasonal-prizes-x2.sql`). Weekly leaderboards are date-filtered by ISO week in the Europe/Warsaw timezone; all-time records stay stored separately.

### Filler

Filler (`supabase/filler.sql` + `supabase/functions/filler-action`) is a **1v1 territory flood-fill** game (the classic 1990 Gamos Ltd DOS game, „7 Colors" internationally) added 2026-08 as arcade-only via „Wszystkie Gry"; the seasonal promotion is deployed and armed but **scheduled for no week** — the 2026-08-10 slot it had been given was reassigned to „Kulki G6" on 2026-08-05, because as the only PvP game it needs two willing players online at the same time and that is a bad bet for an unsupervised week. Everything below is live the moment a Filler week is scheduled. Unlike every other seasonal/arcade game, it is **server-authoritative for every move** (Poker/Roulette/Wheel model, not the client-replayed-tick-simulation pattern) — a "vs bot" match is just a 2-seat match where seat 1 is `(user_id NULL, is_bot true)` (Poker's `bots.sql` trick), so bot-mode and the public-queue-with-bot-fallback PvP mode share one Edge Function and one pair of tables (`filler_matches`/`filler_match_players`), and there is **zero parity contract** to maintain since the client never runs an authoritative sim. **Bot matches never write an `arcade_scores` row, win or lose** — only PvP results score — the only fix that actually closes a free/risk-free/unlimited-attempts farming vector against a best-score-ever leaderboard. ⚠️ The playfield is a **DIAMOND LATTICE** (2026-08-04), i.e. a square grid rotated 45° and re-indexed so it stays rectangular: odd rows sit half a tile right, rows overlap by half a tile, and a rhombus shares a full edge only with the two tiles above and the two below (same-row left/right touch at a point and are NOT connected). This geometry is the **one thing the client and server must agree on** — `neighbors4()` in the Edge Function decides which tiles a fill flows through, `games/filler.js` decides which visibly touch — so shipping `index.html` (auto-deploys on push) without redeploying the Edge Function gives a board whose fills contradict what the player sees. Board is **21×27=567 tiles, 7 colors** (renders 21.5×14, a 1.54:1 landscape field, because h rows are only (h+1)/2 tall); bot practice matches use a **15×19=285** board, ~30% shorter, which is safe to differ because bot matches never score and every dimension-dependent value is derived from the match row. `width × height` must stay odd (majority always decisive, ties structurally impossible). Starting corners are assigned **at random per match** — the lattice's corners have unequal degree, so a fixed assignment measured 43-45% for seat 0. `scripts/filler-balance.mjs` is a bot-vs-bot balance harness (not a parity contract) used to re-derive `FILLER_MAX_MOVES`/`FILLER_MOVES_PAR`/seat fairness whenever the board changes; it takes optional dimensions (`node scripts/filler-balance.mjs 15 19`). `games/filler.js` renders the server's board via a realtime doorbell (filtered per match id) + a 2s poll, as absolutely-positioned clipped rhombi (a CSS grid can't express the half-offset rows), where a diamond-shaped group of four same-color same-owner tiles is drawn as one double-size gem — an exact retiling, purely cosmetic, and the only thing that stops a grown one-color territory reading as a wall of specks. Picking a color paints optimistically client-side before the round trip (`fillerPreviewPick`, explicitly non-authoritative — the server snapshot overwrites it), and `sweepGlobal` is skipped on the `pick_color`/`resign` hot paths. **Seasonal (whenever a Filler week is next scheduled — `supabase/filler-seasonal.sql`): the weekly ranking is a LEAGUE, not a best-single-run** — the only PvP game in the rotation, so "best match wins the week" would mean one lucky win ends your week and you never play a second opponent. `filler_league_week(week_start)` is the single definition (current-week view, all-time view and Monday payout all call it): per match `(win?100:35) + round(score/10)`, multiplied by a **repeat-opponent decay** (1st 100% → 2nd 70% → 3rd 45% → 4th 25% → 5th 15% → 6th+ 10%), plus **60 × distinct opponents**. Losses score on purpose (otherwise you duck the strong players); the decay + breadth bonus are also the anti-collusion device. `filler_scores.opponent_id` is a real indexed column because the ranking groups on it. In seasonal mode the queue waits `FILLER_QUEUE_SEASONAL_MS` (150s) instead of 18s before the bot fallback (a bot match scores nothing there), and `state` returns a `waiting[]` nick list the panel shows as a „ktoś czeka — Dołącz" nudge. The frontend does no ranking math; `SEASON_LIVE_VIEW_SOURCES` in `index.html` routes both Filler and Bug Jumper's pre-aggregated `_current_week` views into the seasonal podium. Full design (schema, the legal-color rule, matchmaking races, the self-healing sweep + cron backstop, the bot heuristic, the score formula) is in docs/filler.md — read it before touching any of this.

### „Kulki G6" (`bubble_breaker`) — full seasonal game since 2026-08

**Bubble Breaker**, the Windows Mobile puzzle that shipped on every Pocket PC (the Dell Axim X51 one). A 15×15 board of 5-colour balls; tap a group of touching same-colour balls to pop it, balls fall into the gap, empty columns slide left, and the round ends when no pair is left. Score is **n×(n−1) per group**, so it is deliberately superlinear — one group of 10 (90) beats five groups of 2 (10) — plus **+1000** for clearing the board. Lives in `games/bubble-breaker.js`; the panel is `#seasonal-game-bubble-breaker`; seasonal SQL is `supabase/bubble-breaker.sql` + `supabase/functions/bubble-breaker-action`.

**Promoted from arcade-only 2026-08-05, debuts via override on the week starting 2026-08-10.** The promotion was cheap precisely because the sim was written pure and deterministic from day one — the Edge Function is a transcription of the fenced PARITY BLOCK, not a rewrite. **One panel node serves both modes**: `loadSeasonalTab()` shows it in place during its season (server-issued seed + round row + `submit`), `selectAllGame()` moves the same node into `#ag-game-slot` for a free arcade run (local seed, no round row, `record_arcade_score`, capped at 12000 in `arcade.sql`). The existing `#ag-game-slot` CSS already hides the seasonal aside/prizes there, so no new CSS was needed.

Four things worth knowing before editing it:

- **No tick clock — the replay is just `(seed, [tapped indices])`.** The cheapest replay in the rotation. Every tap must be a legal pop or the whole submission is rejected, and a round is only scoreable once no pair remains (`st.over`); quitting early is allowed but not submittable, or the best play would be to stop the moment the score peaks.
- **⚠️ That also makes it the one game where an offline solver over the server-issued seed is cheap.** Wall-clock is the guard: `BB_MIN_MS_PER_MOVE` (150 ms) × pops must have elapsed, with `BB_TIME_GRACE_MS` (1500 ms) of slack. Never remove this without replacing it — the sim itself has no notion of time and would accept 60 optimal pops in one millisecond.
- **Input is one rule, two behaviours.** A tap on the *currently selected* group pops it; anything else only selects. A mouse selects by hovering, so desktop pops on a single click; a finger has no hover, so touch naturally falls back to the original's tap-to-select / tap-again-to-confirm. Don't "simplify" this into two code paths.
- **The sim is pure and deterministic on purpose** (`bbInitState`/`bbPopAt`: no DOM, no network, no `Date`, seeded LCG), and `games/previews.js` runs the *real* game for the picker card rather than a look-alike. Keep that property. The `ids` array is cosmetic (it lets the renderer animate a ball from where it was to where it landed) but is drawn from the same seeded stream so a replay stays identical.

**Parity:** the block between the `// ── PARITY BLOCK START` / `END` fences in `games/bubble-breaker.js` and `supabase/functions/bubble-breaker-action/index.ts` must stay byte-for-byte equivalent (rendering constants sit deliberately *below* the fence). `scripts/bb-parity.mjs` is unlike the other harnesses: it **extracts and evaluates both blocks from the source files** instead of carrying a third hand-transcription, then checks them both textually and by fuzzing tap policies. Hero `score_bonus` is `effect_value × 10` here (not ×1 like Tetris) because a good run scores 400-900. Full design in docs/seasonal-games.md.

### Seasonal games

The seasonal tab hosts one rotating arcade game per Monday-start week (Europe/Warsaw). `SEASONAL_ANCHOR_WEEK_START` plus `SEASONAL_ROTATION` in `index.html` derive the active game for any future week; `SEASONAL_OVERRIDES` can replace a specific week; `getCurrentSeasonalEntry()` picks the active game and `loadSeasonalTab()` shows the matching `seasonal-game-*` panel. Current rotation: `whack_boss` → `bug_jumper` → `flappy_pants` („3 Pary Spodni") → `snake` → `invoice_horde` („Najazd Ticketów") → `var_patrol` (VAR Patrol) → `egg_catch` („Łap Jajka") → `super_mariusz` („Super Mariusz") → `popup_panic` („Zamknij Popupy!") → `tetris` („Tetris G6") → `healer_dungeon` („Uzdrowiciel G6") → `filler` (Filler) → `bubble_breaker` („Kulki G6") → `saper` („Saper Maraton") → `arkanoid` („Arkanoid G6"). Overrides: Snake 2026-06-15, VAR Patrol 2026-06-22, Najazd Ticketów 2026-06-29, Łap Jajka (debut) 2026-07-06, Super Mariusz (debut) 2026-07-13, Zamknij Popupy! (debut) 2026-07-20, Tetris G6 (debut) 2026-07-27, „Uzdrowiciel G6" (debut) 2026-08-03, „Kulki G6" (debut) 2026-08-10, Bug Jumper: Dynamic Course 2026-08-17, „Saper Maraton" (debut) 2026-08-24, „3 Pary Spodni" 2026-08-31, three PINS 2026-09-07/09-14/09-21 (flappy_pants/snake/invoice_horde), „Arkanoid G6" (debut) 2026-09-28. (No more `office_grand_prix`; that racer was fully removed 2026-07-23.)

**The four weeks from 2026-08-10 were planned in one batch on 2026-08-05** — „Kulki G6" (debut) → Bug Jumper: Dynamic Course → Super Mariusz → „3 Pary Spodni". Two decisions are worth not re-litigating: **Filler was deliberately pulled** out of the 2026-08-10 slot it had been given (it stays in `SEASONAL_ROTATION` so its awards/history keep rendering, but is scheduled for no week) because as the only PvP game it needs two willing players online simultaneously, which is a bad bet for a week nobody is supervising; and the three non-debut slots were picked from **measured engagement**, not taste — Bug Jumper (261 in-season rounds, and its thrice-postponed Dynamic Course relaunch was already owed a slot), Super Mariusz (417 rounds, the highest any game's week has ever seen), and „3 Pary Spodni" (the most replayed game in the free arcade long after its own season ended: 45 rounds across 7 players). Bug Jumper's relaunch has now been pushed a THIRD time (Tetris → Uzdrowiciel → the Kulki debut).

**2026-08-22: the week of 2026-08-24 was changed from a Super Mariusz encore to the „Saper Maraton" DEBUT** (see its bullet above and docs/seasonal-games.md). Minesweeper is the one office classic the rotation never had, a run is 90 seconds, and — unlike Filler — it needs exactly one willing player, which is what an unattended week can actually rely on. The displaced encore costs nothing: Super Mariusz has already had two weeks and stays in the free arcade. ⚠️ Appending a 14th game also reshuffled every **non-overridden** week, which is how 2026-09-07 became a second `flappy_pants` week directly after the 2026-08-31 override — add an override there if back-to-back repeats matter.

Note that appending `tetris`, then `healer_dungeon`, then `filler`, then `bubble_breaker`, then `saper`, then `arkanoid` to `SEASONAL_ROTATION` changed the modulus 9 → 10 → 11 → 12 → 13 → 14 → 15, so every non-overridden week keeps shifting — `seasonal_game_for_week()` in `season-award-gating.sql` mirrors both the current array and the current `% 15`. ⚠️ **Appending a game reshuffles weeks that were already played — and the one being played.** When `arkanoid` went in (2026-09-22) the running week 09-21 would have flipped from Najazd Ticketów to Snake mid-week, moving its Monday payout onto the wrong game, so 09-07/09-14/09-21 were pinned as overrides holding the games they already had. Do the same (pin every derived week up to and including the current one) the next time a game is appended. The derived schedule currently lands **Filler on 2026-11-16** — the PvP game deliberately kept out of unattended weeks — so that week wants an override before it arrives. **That SQL function is what gates the Monday payout crons, so an override that exists only in `index.html` means the week is played and then nobody is paid.**

Each seasonal game mirrors the same stack:
- `<game>_rounds` / `<game>_scores` / `<game>_weekly_awards` tables, `<game>_current_week` / `_all_time` / `_recent_awards` views, an `award_<game>_week()` SECURITY DEFINER payout (**1000/500/200** — doubled from 500/250/100 on 2026-07-26), realtime publication, and a `pg_cron` job at `'5 0 * * 1'` (Monday 00:05 UTC) that pays the previous week's top 3. The cron jobs are season-gated by `seasonal_game_for_week()` (`supabase/season-award-gating.sql`) so off-season rounds never trigger a payout — **that SQL function mirrors `SEASONAL_ANCHOR_WEEK_START`/`SEASONAL_ROTATION`/`SEASONAL_OVERRIDES` from `index.html` and must be updated and re-run whenever the rotation or overrides change**.
- An Edge Function (`<game>-action`) with `state`/`start`/`submit` actions. The browser cannot write score tables (RLS grants SELECT only); the function owns inserts via `SUPABASE_DB_URL`, caps the per-round score, enforces the round expiry window, and applies hero score bonuses only for games that support them.
- Frontend: a canvas runtime (`new<Game>Runtime`/`<g>Draw`/RAF loop) plus `invoke*`/`load*`/`render*` helpers, wired into `loadSeasonalTab()` and the realtime subscriptions. **`loadSeasonHistory()` needs no per-game edit** — it derives its `<game>_recent_awards` queries from `SEASONAL_ROTATION` (2026-08-03). It used to be three hand-maintained parallel 10-element lists, and `healer_dungeon` + `filler` were simply never added to them, so their payouts ran and then appeared nowhere in the UI. Two small override maps carry the only deviations: `SEASON_AWARD_SOURCES` (bug_jumper reads the raw awards table for `course_id`; super_mariusz also selects `completion_ms`) and `SEASON_LIVE_SOURCES` (the live „na żywo" pending podium — the default tiebreak column is `accuracy`, which every `*_scores` table has **except `filler_scores`**). ⚠️ Selecting a column that doesn't exist makes postgrest return an error rather than rows, and the aggregator reads a null payload as "nobody has played yet" — so a wrong entry blanks that game's podium for its whole week instead of failing loudly.

Bug Jumper relaunched 2026-07-27 with a **dynamic course** (`course_id = 'bug_jumper_dynamic_v1'`): each round draws a random seed and both client and server derive an identical procedural course from it (`bjGenerateCourse`/`generateCourse`), verified 0-mismatch by `scripts/bj-parity.mjs`. `bug-jumper-top5-scoring.sql` changed weekly ranking to average each player's **top 5 scoring rounds** rather than their single best. The board was redesigned 2026-07 to **56×32** with a fully dynamic corridor (drifting center, 3 obstacle kinds: crawl/bounce/block) and a real Fullscreen API toggle.

„3 Pary Spodni" (Flappy Pants) is a Flappy Bird clone with 3 lives; score = obstacles passed, persists across lives. Its round is validated by a server-side replay of `(seed, flapEvents, elapsedMs)`, so **the browser sim runs a fixed 16 ms step (`FP_TICK_MS` ↔ `REPLAY_TICK_MS`) and every flap timestamp is on the SIM clock, not the wall clock** — verified by `scripts/fp-parity.mjs`, which drives the real `games/flappy-pants.js` in a vm against the real `replayFlappy()`. That replay shipped 2026-06-14, after the game's only season, and was first exercised in the week of 2026-08-31 — where it stored **every** run as `base_score` 0: the client stamped flaps from the round's start on the server clock while its own sim started a network round-trip later, so the replay flew the first ~0.5 s with no input and burned all three lives before the first flap arrived. Two boundary rules keep it exact: a flap's `atMs` must be strictly greater than the last simulated instant (`<=` in the replay's matching test would otherwise apply it one tick early), and `elapsedMs` carries one extra tick of slack so the replay's last, shortened step still sees the collision the player died on.

Snake is a 20×20 seeded-grid game; the browser submits direction changes and `snake-action` replays them server-side. Ranking: score desc, then shorter duration, then earlier submission.

„Najazd Ticketów" (`invoice_horde`) is an arena-survivor: dodge homing support tickets, an auto-firing script kills the nearest one, periodic „CF" mini-bosses. One life, deterministic 80ms-tick sim (seeded LCG), client logs only direction changes, server replays for the trusted score. Ranking: score desc, then longer survival time.

VAR Patrol (`var_patrol`) is a rapid football-officiating judgement game — the server issues a schedule of scenarios and validates `{index, answer, reactionMs}` against a shrinking decision window. Ranking: score desc, accuracy desc, then earlier submission.

„Łap Jajka" (`egg_catch`) is an egg-catching LCD-style game (4 chutes, 4 basket positions), deterministic 100ms-tick sim, 3 broken eggs ends the run. Ranking: score desc, then earlier submission.

„Super Mariusz" (`super_mariusz`) is a one-life HARDMODE speedrun platformer on a fixed course (`course_id='super_mariusz_v2'`); ranking is **progress first, time second** (stored score = tile-column progress, so DNFs can still win). Deterministic 50ms-tick fixed-point sim; hero score-bonus items apply as a time bonus (subtracted from `completion_ms`), not points.

„Zamknij Popupy!" (`popup_panic`) is a ~30s popup-closing frenzy: close popups before 12 are open at once or a malware popup's deadline expires. Deterministic 100ms-tick sim; DOM-rendered (not canvas). Ranking: score desc, then earlier submission.

„Tetris G6" (`tetris`) is classic 10×20 Tetris with a 7-bag randomizer. Scoring is deliberately small and line-only (not multiplied by level); level rises every 10 lines to a max of 15, and gravity speeds up per the Tetris Guideline. Deterministic 50ms-tick sim (seeded LCG for the bag). Rendering is canvas with the whole HUD drawn on it; on handheld the round always opens fullscreen.

All nine games share one parity discipline: the constant blocks and transition functions in `index.html`/`games/*.js` and the matching Edge Function must stay **byte-for-byte equivalent**, each verified by its own `scripts/*-parity.mjs` fuzzer. Full tuning history, exact constants, and balance rationale for each game are in docs/seasonal-games.md.

### „Uzdrowiciel G6" (`healer_dungeon`) — full seasonal game since 2026-08

A WoW Classic-style **dungeon healer** sim: a five-man party (tank/you/3 dps) faces an endless dungeon where every pull is harder and every fifth pull is a boss; the run ends only when **you** die. Score is **points** (depth + healing efficiency + a flawless-pull bonus + tempo), not pulls cleared. Lives in `games/healer-dungeon.js`; seasonal SQL is `supabase/healer-dungeon.sql` + `supabase/functions/healer-dungeon-action` (a byte-for-byte transcription of the fenced PARITY BLOCK in the game file, verified by `scripts/hd-parity.mjs`).

Two things are unusual vs every other seasonal game: **(1)** the class (`HD_CLASSES`: druid/priest/paladin/shaman) is chosen client-side before the round starts and stored on the round row alongside the seed — safe because all four classes are balance-asserted to the same HP-per-mana. **(2)** a round has no natural "clean" end — it only stops on death or the `HD_MAX_TICKS` replay-cost safety cap, both valid submissions.

Controls are **mouseover/click-casting** (hover or click a raid frame, then press 1/2/3), not target-then-cast — the target step was what made healing feel like queuing orders instead of reacting. A fallen ally doesn't end the run, only the healer's own death does. The console is a fit-to-viewport takeover (`.hd-fit`) at a fixed virtual resolution that scales to any window size, and never scrolls.

`scripts/hd-balance.mjs` is the balance harness: it asserts the 5-second mana rule, cross-class HP-per-mana parity (≤3%), boss telegraph timing, and that a good run lands in the target 7-11 pull band, among many other sim invariants.

**Full sim design, every tuning revision's rationale, controls/scoring detail, and the complete balance-harness spec are in docs/healer-dungeon.md — read it before touching any `HD_*` constant, the class tables, or the sim's transition functions.**

### „Wszystkie Gry" picker (`#tab-all-games`)

The free-forever arcade tab is a storefront: a **🏆 Klasyfikacja generalna** medal-table podium (all-time golds/silvers/bronzes per game) followed by one wide card per game. Each card runs a **live gameplay preview** that renders through the game's own draw function via a per-frame global-hijack pattern (`games/previews.js` swaps the real canvas context/runtime for a throwaway one, so a preview can never drift from the real game and never starts an actual round). Records show two columns (all-time + this week, top 5 each). Cards are `role="button"` divs (not `<button>`s), since previews mount real DOM inside them. Full preview-engine mechanics and per-game bot policies are in docs/all-games-picker.md.

### World Cup betting (Mundial)

The `⚽ Mundial` tab is **fixed-odds betting vs the house** on FIFA World Cup 2026 matches — unlike the peer-pooled CPMM markets, it mints/burns coins like slots/roulette. A player picks 1/X/2; bookmaker decimal odds are **locked at bet time**, a win pays `floor(stake × locked_odds)`, losses are burned. Displayed "szansa" is the de-vigged implied probability. **One bet per match** per user, enforced server-side inside the `placeBet` transaction.

Two providers, on purpose: **The Odds API v4** supplies events/odds; **football-data.org v4** supplies settlement results. Both are written only by `football-action` (`verify_jwt=false`, JWT checked manually in `bet`) — the browser never sends odds. **Why two providers:** the 1X2 market settles on the **90-minute regulation result**, ignoring ET/penalties, but The Odds API's `/scores` can't distinguish a regulation result from an ET one. football-data.org's `score.duration`/`score.regularTime` fields give the true pre-ET score, so `loadRegularTimeScores()` derives the result score, display score, and status (`FT`/`AET`/`PEN`) from there. `canonicalTeam()`/`TEAM_ALIASES` normalize team-name spelling differences between the two providers (matches are looked up by team name, not id, since the providers use different id schemes).

An every-6h `pg_cron` job (`football_hourly_sync`) posts `{action:'cron'}` with an `x-cron-secret` header. It (1) syncs events+odds from The Odds API, (2) settles finished matches from football-data.org, (3) **voids+refunds** any match still `open` >12h past kickoff with no result (cancelled/API gap/etc — settle always runs first so a genuine result wins over a void). An admin-only `void` action does the same immediately for one match.

Config (Edge Function secrets): `ODDS_API_KEY`, `FOOTBALL_DATA_API_KEY`, `FOOTBALL_CRON_SECRET`, optional `ODDS_SPORT_KEY`/`ODDS_REGIONS`/`ODDS_BOOKMAKER`/`FOOTBALL_DATA_COMPETITION`. Frontend: `invokeFootball`/`loadFootball`/`renderFootball`/`buildMatchCard` + a `#fb-overlay` bet modal, wired into realtime on `football_matches`/`football_bets`. Manual corrections to already-settled matches use `coin_transactions.reason='football_correction'`.

### Collaborative canvas (Wspólne Płótno)

The `🎨 Wspólne Płótno` tab is a shared **r/place-style pixel board** — one common **192×108** (16:9) grid that the whole office paints together, rendered near-fullscreen with a thin instructions/toolbar bar on top. Unlike a game, there is no score, and (2026-07-14) **every pixel is free for everyone, unconditionally** — the earlier 1-free-pixel-per-2h / pay-1-coin-on-cooldown split is gone (`supabase/canvas-free-for-all.sql` supersedes that RPC; the `canvas_cooldowns` table is left in place, just unused). A fixed ~16-color palette keeps the canvas coherent. The nav tab carries a small „Nowość" pill (`.nav-new-badge`, shared with the Targowisko nav button).

Unlike Mundial/poker, the write path is a **direct `SECURITY DEFINER` RPC** (`place_pixel(p_x, p_y, p_color)`), not an Edge Function — it mirrors the garden `water_plant()` cooldown pattern (minus the cooldown, now). The browser has SELECT-only access to `canvas_pixels`/`canvas_cooldowns` and calls `sb.rpc('place_pixel', …)`; the RPC validates bounds + hex color, then upserts the pixel `ON CONFLICT (x,y) DO UPDATE` (last-write-wins, so simultaneous clicks resolve to one final color).

The **grid bounds (192×108) are duplicated** between `supabase/canvas.sql` (the RPC) and `index.html` (`CANVAS_W`/`CANVAS_H`) — keep them in sync. Frontend: `loadCanvas`/`drawCanvas`/`placePixelRpc`/`onCanvasTap` + `cvSetup` pan/zoom pointer handling (`cv*` helpers), wired into `switchTab('canvas')` and a `canvas_pixels` realtime subscription (`applyCanvasPixelChange`) that patches single cells without a full reload. `fetchAllCanvasPixels` paginates the initial SELECT in deterministic `(x,y)` order because Supabase caps one Data API response at 1,000 rows; changes arriving during that multi-request load are replayed onto the completed snapshot so fresh pixels are not lost.

### Office chat („Czat G6", ICQ-style rail)

An always-on, ICQ-styled chat docked to the **left edge**, present on **every tab** — the `#chat-rail` markup lives inside `#screen-app` but **outside every `#tab-*` div** (right after `</nav>`), so `switchTab()` never unmounts it and it's naturally hidden until login. **Permanently open, and part of the page layout (2026-08-28)** — there is no launcher pill and no collapse button, and the chat is the lower of **two** modules in a left **side rail**: **Biuro** (every non-admin player, including you, sorted by last activity with a relative timestamp) and then the chat itself. Above 900px the rail is a column fixed to the left edge (`--chat-w`, 320px) that starts **below** the header and nav — both stay full-width and own the top of the screen — at `calc(var(--nav-top) + var(--nav-h))`, the same pair `syncNavOffset()` already measures for the sticky nav. The `padding-left` that keeps content clear of it is on `#screen-app > [id^="tab-"]`, **not** on `#screen-app`, or the chrome would be indented too; `.toast`/`.update-banner` are re-centred over the content column for the same reason. Stacking is 35: below the nav (40) and header (41). Below 900px a 320px column would leave a phone nothing to render into, so `--chat-w` drops to 0 and the rail falls back to a bottom dock (42vh, with a matching `padding-bottom` so the page can scroll clear of it) — and **there, and only there, it collapses**: it starts SHUT on every load (`setRailOpen(!railIsMobile())` — „hidden by default” means every visit, not „until you once opened it”), a 🌼 Czat launcher pill reopens it and a ✕ in the chat header shuts it again. Both controls are `display: none` above 900px, where the rail is a column of the page and there is nothing to collapse into; the ✕ rule has to be `#chat-rail`-scoped because `.chat-hbtn`'s own `display: inline-flex` is defined further down and would win on source order. ⚠️ Notifications are the point of collapsing: while it is shut, an incoming **room** message (normally ambient — you can watch it land) also badges the pill, toasts and beeps, on top of the whisper badges that always did. `railVisible()` — not the raw `railOpen` flag — gates that, because the collapsed CSS lives inside the media query, so above 900px the panel is on screen whatever the flag says, and a stale flag must not make a visible panel toast its own messages. The look is deliberately flat (one solid column, hairline rules, no shadows/rounded cards/backdrop blur): it is on screen permanently, so it has to recede.

**The Biuro list is the roster — there is no Kontakty tab.** Clicking a row opens `#pu-overlay`, a spare per-player window: online state, when they were last active, their last 20 events (`coin_transactions` + `game_transactions`, both readable for every player, labelled off `ACT_COIN_REASONS`/`ACT_GAME_LABELS` plus a `SIDE_ACT_REASONS` map carrying the daily yields the office-wide feed omits on purpose), and — for anyone but you — one **💬 Napisz wiadomość** button that opens the whisper thread. So `setChatView()` now has two views, `room` and `thread`, and `chatRenderRoster()` is a one-liner that re-renders the Biuro list; presence changes, unread badges and view switches all funnel through it. ⚠️ `#pu-overlay` is listed alongside `#chat-rail`/`#chat-toasts` on the `--c-*` palette rule: the window is painted from that palette but sits outside the rail in the DOM, and an unresolved `var()` there fails silently (a transparent presence dot, not an error).

**„Last active" is `profiles.last_active_at`**, stamped on write by the triggers in `supabase/last-active.sql`, so the list costs one indexed read of eleven rows. It refreshes on any balance move (a `MutationObserver` on `#header-coins` — a moved balance is the cheapest signal something happened, and mirroring the header beats patching the ~20 `setText(headerCoins, …)` call sites across `index.html`, `tabs/*.js` and `games/*.js`, one of which would eventually be missed) and on every presence flip, throttled by `SIDE_PEOPLE_MIN_MS` (60s). `sideLoadPeople()` is the **single** roster loader — it owns both `sidePeople` (the Biuro list, me included) and `chatRoster` (that minus me, the whisperable set), so `initChat()` awaits it instead of running its own `profiles` read. ⚠️ **`player_activity_stats()` is still wired in as a fallback and must stay**: `index.html` deploys itself on push while the SQL is pasted into Supabase by hand, so there is always a window where the frontend is live and the column does not exist — one failed select flips `sidePeopleColumn` and the rail keeps working off the aggregate. The throttle stamp is taken on *attempt*, not on success, or two dead reads would turn every presence flip into a retry. Sorting is by last activity, **not** by online state, or the list would reshuffle every time someone opened a tab.

**Reactions and naklejki** live in `tabs/chat-reactions.js`, a lazy module registered in `TAB_MODULES` as `chatfx` (not a tab) that `initChat()` fetches after the chat is already usable. `index.html` keeps only no-op `chatFx*` stubs plus hooks in `chatMsgNode` (`chatFxDecorate`), the two message loaders (`chatFxLoadFor`), `chatIngest`'s toasts (`chatFxPreview`, so a sticker toasts as its label, not `:st:code:`), the `chat_reactions` realtime handler and `resetChatState`. Hover ☺ (desktop) / long-press (touch) on a message opens the picker; the 😜 button injected next to Send sends a sticker through `chatSendBody()`, the send path shared with the text input. The picker `#cfx-pop` lives on `<body>`, so it is on the `--c-*` palette selector alongside `#pu-overlay`. See `supabase/chat-reactions.sql` above.

Backend is `supabase/chat.sql` (see the file list above) — the **direct SECURITY DEFINER RPC** pattern (like `canvas.sql`), not an Edge Function. Presence is a `last_seen` heartbeat exactly like roulette seats: `chatHeartbeatPing()` calls `chat_heartbeat()` every `CHAT_HEARTBEAT_MS` (25s) while the tab is visible (plus on `visibilitychange`); a user is online until `last_seen` is older than the server's `CHAT_STALE` (90s), and the `chat_presence.online` flip is what the frontend turns into ICQ „wszedł/wyszedł z sieci" toasts + a local (non-persisted) system line in the room, with an optional off-by-default WebAudio beep. **RLS is the whisper boundary** (public OR `auth.uid() IN (sender, recipient)`), enforced for realtime too. Delivery rides the shared `db-changes` channel: `chat_messages` INSERT → `chatIngest()` (de-dupes by id, bumps unread, toasts), `chat_presence` `*` → `chatOnPresence()` (diffs vs the local `chatOnline` set, so `last_seen`-only heartbeat ticks are ignored). Messages render via `el()` (text nodes — no innerHTML), so bodies are XSS-safe. Frontend state (`chatRoster`/`chatOnline`/`chatRoomMsgs`/`chatThreads`/`chatUnread`) is reset in `doLogout()` after a best-effort `chat_set_offline()`. Keep the mirrored constants (`CHAT_HEARTBEAT_MS`↔`CHAT_STALE` ⅓ rule, `CHAT_MAX_LEN`, ≥1s send throttle) in sync with `supabase/chat.sql`. The 25s beat itself writes only to the unpublished `chat_presence_beats` table (`chat-presence-quiet.sql`), so `chat_presence` — and therefore realtime — sees a write **only** on a genuine online/offline flip; don't "simplify" that conditional upsert back into an unconditional one.

### Farma (farm grid + card economy)

**Farma** is the top-level nav tab **🌱 Ogródek** (`#tab-farm`) — a shared **13×4** DOM-grid field of individually-owned tiles, replacing the old zen-garden tab (migrated plant tiles reuse the original `buildPot` code). Core loop: **buy a tile** (escalating BURN price) → **open a loot box** (100🪙, `open_farm_lootbox`, 3 distinct rarity-weighted cards) → **level cards** (consumes dupes+coins) → **plant** on an owned tile (server-timed growth, 24h floor) → **harvest** (mints crop units that rot 5 days later) → **sell to the NPC** at a fluctuating „stalk market" price (rolls twice daily, dips per sale, recovers ~12%/hr).

**Phase 2 (live):** serialized NFT cards (`farm_nft_instances`, unique serial + persona name per instance) drop from the normal loot box at low weight until their edition sells out; NFTs level by **merging** two same-species same-level instances (`level_up_nft`), never by spending coins alone.

**Phase 3 (live):** P2P resale of cards/NFTs/tiles via `farm-marketplace.sql`, reusing the Targowisko bid/escrow engine.

`FARM_W`(13)/`FARM_H`(4) must mirror `supabase/farm.sql`; every farm coin reason must stay in sync with `economy-stats.sql` and `leaderboard-net-worth-items.sql`. **Full gameplay/economy design (grid & tiles, land tax, lootbox odds, card/NFT leveling, „stalk market" pricing, weekly contracts, marketplace resale, balance-tuning knobs) is in docs/farma.md — read it before touching any farm RPC or pricing constant.**

### Ogródek / nav UI conventions

A few cross-cutting conventions worth knowing before touching farm UI: **one card per `crop_type`, not per plant** (shared crops like `seasonal_bloom` get one `farm_market` row and one 📈 Cennik card, listing contributing plants underneath — don't render a duplicate card per NFT edition), and **any code naming a crop pile from a `crop_type` must go through `farmCropIdentity()`** — never `fmDefs.find(d => d.crop_type === ct)`, which labels a pooled pile with an arbitrary contributing plant the player may not even own. In 📖 Katalog, **bred hybrids never show a species-wide level or generic profit ladder**: their yield/grow/value are stored per instance from the exact parents, so precise stats belong in 🎒 Mój Majątek (a planted hybrid may still show its real instance-based income). **🎒 Mój Majątek is a portfolio dashboard**: net farm value/action alerts first, crop sale + the Top-3 all-owned `Dzisiaj`/`Przy pułapie` profitability ranking second, then context-aware collapsible groups for plant cards, NFT instances, plots and special items. The ranking includes planted/listed NFTs and uses one shared full-cycle gross projection with server-order level/hybrid/land/NFT rounding, the 24h floor, whole-harvest `farmSellQuote` batch impact, Collector Reputation and eligible weekly-contract bonuses; it never substitutes remaining grow time or tax repayment. Breeding has only a compact `Skrzyżuj` CTA in the NFT group; its rules/destruction warning/synergy and the collapsed office hybrid feed live in the breeding modal. **A serialized NFT is drawn with `farmNftEmoji(n)`/`farmNftIconFor(species, serial, speciesEmoji)`, never `def.emoji`** (2026-08-07) — icons are per species, which breaks down for `wild_hybrid`, the nakład-500 catch-all where one 🌈 stood in for every wild hybrid ever bred; each serial now maps into the **append-only** `WILD_HYBRID_ICONS` by `(serial-1) % length`, client-side and deterministic (no server column). `farmListingEmoji()` (Targowisko, via `fmNft`) and `farmTileCropEmoji()` (planted tile, via `planted_instance_id`) exist because those surfaces hold no instance row; species-level surfaces keep 🌈. **`plCount(n, one, few, many)`** handles Polish 1/2-4/5+ plurals for any counted noun. **Wood is scenery only** (`.notice-board`, `.plot-sign`, `.farm-divider`) — chrome that carries controls uses the neutral `--r-*`/`--sh-*` tokens; hub-modal panels avoid boxes-inside-boxes. **`.nav-tabs` are sticky at every width** via `syncNavOffset()` (a `ResizeObserver` publishing `--nav-top`/`--nav-h`, since the mobile header wraps to two rows); the popover stacking order is `.gp-details-popover`/`.ftbl-menu` 30 < `.nav-tabs` 40 < `header` 41 < modals 100+. Full detail (portfolio/groups, NFT explorer/modal split, profitability projection, tax-card layout shape, mobile nav-dropdown behavior) is in docs/farm-ui-conventions.md.
